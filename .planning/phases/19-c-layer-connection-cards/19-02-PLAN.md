---
phase: 19-c-layer-connection-cards
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/components/LayerText.tsx
autonomous: false
requirements: [CONN-01, CONN-03]
nyquist_compliant: true

must_haves:
  truths:
    - "Hovering/pinning a real c-layer atom on the live canvas shows a card listing the atoms it is bonded to, derived from the incidentPairs the ConnectionText atom-token site now attaches."
    - "In a multi-component molecule (toluene co-crystal) a component-2 atom/hyphen/parenthesis hover shows per-component numbers + '(component 2)', because ConnectionText now threads cumOffset/fragIdx onto every c-layer SubHover."
    - "The canvas highlight is unchanged from Phase 15 — SubHover payloads still carry global canonicals; the new fields are display context only."
    - "A human chemist confirms the live atom/hyphen/branch card strings (single-component and co-crystal) are notationally accurate before the phase is verified."
  artifacts:
    - "src/components/LayerText.tsx — ConnectionText atom-token site attaches incidentPairs (global) computed from segmentBonds; all atom/bond/branch SubHovers carry fragmentOffset + componentIndex; branch SubHovers (open AND close) carry branchPoint."
  key_links:
    - "incidentPairs are computed with the SAME offset/canonicalFn the hyphen path already uses (LayerText.tsx:194-202) — global canonicals, never de-offset before storing."
    - "fragmentOffset = cumOffset and componentIndex = fragIdx at the ;-split site (in scope at :289-307); the pure-2* and single-component paths use fragmentOffset:0/componentIndex:0 (single-component cards unchanged, per the design)."
    - "BOTH the open-paren and close-paren branch SubHovers get branchPoint + fragmentOffset + componentIndex (research open question A2) so hovering either paren shows the identical card; the close site reads the open token's attachLocal/_bondPairs and has offset/canonicalFn in scope."
  prohibitions:
    - "Never write a de-offset (local) number into a SubHover field — atoms/pairs/incidentPairs/branchPoint stay GLOBAL (the auxMap key)."
    - "No change to highlightUtils.ts, the store, the provider, or <Editor>; no new parser (reuse the exported segmentBonds)."
    - "No bond-order/H/geometry inference at the construction site — only adjacency from segmentBonds."
---

<objective>
Wire the new SubHover display-context fields at the `ConnectionText` construction sites so the
plan-01 cards receive real data on the live canvas: attach each atom token's incident bond pairs
(global), and thread `fragmentOffset`/`componentIndex` (and `branchPoint` for branches) onto every
c-layer SubHover. Then run the full suite and the human chemist accuracy gate.

Purpose: plan 01 defined the type fields and the card consumers and tested them against
parsed-projection literals. The fields are still empty on the live SubHovers — `ConnectionText`
must populate them (the CONN-01 data gap + CONN-03 per-component context). No card text changes;
this is pure render-site data wiring.

Output:
- `ConnectionText` atom-token site computes `incidentPairs` via the exported `segmentBonds` (filtered
  to the atom), with the hyphen path's offset/canonicalFn — global canonicals.
- All atom/bond/branch SubHovers carry `fragmentOffset`/`componentIndex`; branches carry `branchPoint`.
- Full suite green + the human chemical-accuracy verify gate captured for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/superpowers/specs/2026-06-29-c-layer-connection-cards-design.md
@.planning/phases/19-c-layer-connection-cards/19-RESEARCH.md
@.planning/phases/19-c-layer-connection-cards/19-01-SUMMARY.md
@src/components/LayerText.tsx
@src/lib/parseInchi.ts
</context>

<artifacts_this_phase_produces>
No new symbols — this plan only POPULATES the plan-01 SubHover fields at the render site:
- atom token → `incidentPairs` (global, from `segmentBonds(tokens)` filtered to `token.localN`, offset/canonicalFn applied) + `fragmentOffset` + `componentIndex`.
- hyphen token → `fragmentOffset` + `componentIndex` on the existing `{kind:'bond', endpointPairs}` hit.
- open/close paren → `branchPoint` (global, from the open token's `attachLocal` + offset/canonicalFn) + `fragmentOffset` + `componentIndex` on the existing `{kind:'branch', bondPairs}` hits.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Thread fragmentOffset/componentIndex onto all c-layer SubHovers + attach branchPoint to both parens</name>
  <read_first>
    - src/components/LayerText.tsx (ConnectionText :168-310 — the `renderSegment(seg, offset, canonicalFn?)` closure; the atom/hyphen/open/close construction sites at :180-261; the `;`-split loop with cumOffset/fragIdx at :288-308; the pure-2* branch at :273-283)
    - src/lib/parseInchi.ts (the plan-01 SubHover fields incidentPairs/branchPoint; tokenizeCLayerSeg :139-183 — `open.attachLocal` is the branch-point local; segmentBonds — now exported)
    - 19-RESEARCH.md §Pitfall 2 (fragmentOffset/componentIndex reach h-layer kinds only — the two construction loops to fix) and §Open Question 2 (close-paren needs the same fields as open)
  </read_first>
  <action>
    Extend the `renderSegment` closure signature to accept the per-component display context (`fragmentOffset` and `componentIndex`) — pass `cumOffset`/`fragIdx` at the `;`-split call site (:304) and `0`/`0` at the pure-2* call site (:278) and the segment-2* call site (mirror the h-layer choice: single-component and identical-fragment copies use 0/0 per 19-RESEARCH.md §Pitfall 2). At the atom-token site (:180-187): attach `fragmentOffset` and `componentIndex` to the `{kind:'atom', ...hover}` hit. At the hyphen site (:203): add `fragmentOffset` and `componentIndex` to the `{kind:'bond', endpointPairs}` hit. At the open-paren site (:238) and close-paren site (:254): add `fragmentOffset`, `componentIndex`, AND `branchPoint` to the `{kind:'branch', bondPairs}` hit. Compute `branchPoint` as the open token's `attachLocal` run through the SAME offset/canonicalFn the bondPairs use (canonicalFn → `.canonical`; else `attachLocal + offset`) — global canonical (research A2: explicit field, both parens identical). For the close-paren, the open token index is `token.openTokenIdx`; read `attachLocal` off `tokens[token.openTokenIdx]`. Numbers written into SubHover stay GLOBAL — never subtract fragmentOffset here. Commit: `feat(19-02): thread fragmentOffset/componentIndex + branchPoint onto c-layer SubHovers`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <done>Every atom/bond/branch SubHover built in ConnectionText carries `fragmentOffset`/`componentIndex`; both branch parens carry `branchPoint`; all values GLOBAL. `npx tsc --noEmit` and the full suite (`npx vitest run`) are green; highlightUtils tests still pass (highlight unchanged).</done>
</task>

<task type="auto">
  <name>Task 2: Attach incidentPairs to the atom-token SubHover (CONN-01 data gap)</name>
  <read_first>
    - src/components/LayerText.tsx (the atom-token site :180-187; the hyphen path's offset/canonicalFn handling at :194-202 — the exact pattern to mirror for incidentPairs)
    - src/lib/parseInchi.ts (exported `segmentBonds(tokens)` :191-208 — returns pseudo-hyphen tokens with leftLocal/rightLocal; `collectBranchPointBonds` :228-247 — the filter-to-an-atom pattern, generalised here from branch-point to any atom)
    - 19-RESEARCH.md §Pitfall 1 (the 'atom' SubHover carries NO neighbours today — the core CONN-01 gap) and §Don't Hand-Roll (reuse segmentBonds; do NOT write a second adjacency walker)
  </read_first>
  <action>
    At the atom-token site (:180-187), compute the atom's incident bond pairs and attach them as `incidentPairs` (global) on the `{kind:'atom'}` SubHover. Reuse the exported `segmentBonds(tokens)` (NOT a new walker — Don't-Hand-Roll / GAP-15 trap): filter its pseudo-hyphen output to bonds where `leftLocal === token.localN || rightLocal === token.localN`, then map each surviving `[leftLocal, rightLocal]` to global canonicals with the SAME offset/canonicalFn the hyphen path uses (:194-202) — canonicalFn → `.canonical` (use `.canonicals` first entry if present, matching the hyphen path), else `+ offset`. Store the resulting `[number, number][]` as `incidentPairs` (global). This is exactly `collectBranchPointBonds`' body generalised from the branch-point atom to an arbitrary atom — do not re-derive adjacency. The atom card (plan 01) derives neighbours as the other endpoint of each pair. Commit: `feat(19-02): attach incidentPairs to c-layer atom SubHover (CONN-01)`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/__tests__/subTokenInfo.test.ts src/lib/__tests__/highlightUtils.test.ts</automated>
  </verify>
  <done>The atom-token SubHover carries `incidentPairs` (global canonicals) computed via the exported `segmentBonds` filtered to the atom and offset like the hyphen path; no second adjacency parser added; type-check + the c-layer test subset green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The c-layer connection cards are live: the SubHover fields (incidentPairs, fragmentOffset,
    componentIndex, branchPoint) are populated at the ConnectionText render site, so hovering or
    pinning a connection-layer atom number, hyphen, or parenthesis now shows its sub-token card.
  </what-built>
  <how-to-verify>
    1. Run the app: `npm run dev`; open the printed localhost URL.
    2. Single-component: load/draw a molecule whose c-layer has a branch (e.g. alanine
       `c1-2(4)3(5)6`, or load the Caffeine preset). Hover, in the c-layer string:
       - an atom number → card lists the atoms it is bonded to (matches the adjacency you can read
         in the string), titled "Atom"; check a one-neighbour atom and a multi-neighbour atom.
       - a hyphen → card names the two atoms on either side as bonded, titled "Bond".
       - an open or close parenthesis → card names the branch-point atom and lists the branch's
         bond pairs (en-dash, e.g. "2–4"), titled "Branch"; hovering '(' and ')' shows the SAME card.
       Confirm the canvas highlight still lights the correct atoms/bonds (Phase 15 behaviour, unchanged).
    3. Multi-component: draw/load a salt or co-crystal whose SECOND component has a real ring/chain
       in the c-layer (the research's toluene co-crystal, or any salt with a multi-atom anion/cation).
       Hover an atom/hyphen/parenthesis IN COMPONENT 2 → the card numbers match the per-component
       numbers PRINTED in the string (reset after the `;`), and the card shows "(component 2)";
       the canvas highlight still lights the correct atoms.
    4. Confirm NO card claims a bond order, hydrogen count, or geometry; cards speak atom numbers only.
    5. Confirm no loading overlay / canvas re-init flashes on c-layer hover or pin (no-remount).
    This is the v1.5/v1.6 load-bearing chemical/notation-accuracy gate — do not skip it.
  </how-to-verify>
  <resume-signal>Type "approved" if all card strings are notationally accurate and the highlight is unchanged, or describe the inaccurate card (which token, what it said, what it should say).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM → render site | The InChI string + auxMap come from the ketcher-standalone WASM worker; ConnectionText tokenizes the c-layer and builds SubHovers (global canonicals). |
| SubHover → card | Populated fields flow to the pure subTokenInfo module (plan 01), then to React text children (existing Phase 18 path). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-04 | Tampering / state corruption | GAP-2 highlight invariant at the construction site | low | mitigate | All numbers written into SubHover (incidentPairs, branchPoint, endpointPairs, bondPairs, atoms) stay GLOBAL — offset applied via the same canonicalFn the existing hyphen/branch paths use; fragmentOffset/componentIndex are display context only and are never subtracted here. The plan-01 buildSubHoverSpecs guard test proves the highlight still resolves via globals. Diff-scope gate: this plan touches only LayerText.tsx. |
| T-19-05 | Denial of service / no-remount | <Editor> / provider | low | mitigate | No store field, no conditional `<Editor>` render, no provider recreation; render-site data assembly only. Confirmed by the human-verify no-remount check (no loading overlay on hover/pin). |
| T-19-06 | Tampering / XSS | card prose | low | accept | This plan adds no card text and no `dangerouslySetInnerHTML`; the strings are still plain text rendered by the pre-existing Phase 18 path. |

> No package installs — no `T-19-SC` supply-chain item. No schema/ORM files in scope.
</threat_model>

<verification>
- `npx tsc --noEmit` — type-check green.
- `npx vitest run` — full suite green; highlightUtils tests confirm the highlight is unchanged.
- Human chemist gate (blocking): live atom/hyphen/branch cards on a single-component and a
  co-crystal molecule are notationally accurate; per-component numbering + "(component N)" correct;
  highlight unchanged; no bond-order/H/geometry claim; no remount.
</verification>

<success_criteria>
- Every c-layer SubHover built by ConnectionText carries `fragmentOffset`/`componentIndex`; atom carries `incidentPairs`; branch (both parens) carries `branchPoint` — all GLOBAL.
- Full suite + type-check green; highlight behaviour unchanged from Phase 15.
- The live chemist accuracy gate passes (not bypassed).
</success_criteria>

<output>
Create `.planning/phases/19-c-layer-connection-cards/19-02-SUMMARY.md` when done.
</output>
