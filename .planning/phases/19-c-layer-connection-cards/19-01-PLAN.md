---
phase: 19-c-layer-connection-cards
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/parseInchi.ts
  - src/lib/subTokenInfo.ts
  - src/lib/__tests__/subTokenInfo.test.ts
  - src/lib/__tests__/highlightUtils.test.ts
autonomous: true
requirements: [CONN-01, CONN-02, CONN-03, CONN-04]
nyquist_compliant: true

must_haves:
  truths:
    - "Hovering/pinning a c-layer atom number produces a card listing every canonical atom it is bonded to (full neighbour set, enumerated — no range, no truncation); a zero-neighbour atom says 'no bonds recorded'."
    - "Hovering/pinning a hyphen produces a card naming the two canonical atoms it joins; a parenthesis produces a card describing the branch off its branch-point atom and listing the canonical bond pairs it encodes."
    - "In multi-component molecules the card numbers match the per-component numbering printed in the string (reset after each ';', with a '(component N)' marker), while SubHover payloads keep global canonicals so the existing highlight is unchanged — proven by a buildSubHoverSpecs guard."
    - "subTokenInfo produces all three cards in the pure module (no string reconstruction, no parser call); tests use real getInchi() fixtures (ALANINE single-component, MELATONIN_TOLUENE co-crystal) and guard the empty atom-list case."
  artifacts:
    - "src/lib/parseInchi.ts — SubHover gains optional incidentPairs?: [number,number][] and branchPoint?: number fields"
    - "src/lib/parseInchi.ts — segmentBonds exported for the LayerText atom-incident-pair computation in plan 02"
    - "src/lib/subTokenInfo.ts — 'atom' | 'bond' | 'branch' cases return a card (no longer null); bondPairList helper added"
    - "src/lib/__tests__/subTokenInfo.test.ts — :193-205 null-fallthrough block REPLACED with the new non-null contract + atom/bond/branch/component/copy-safety tests"
    - "src/lib/__tests__/highlightUtils.test.ts — c-layer GAP-2 highlight-invariance guard (atom + bond kind)"
  key_links:
    - "subTokenInfo de-offset is DISPLAY-ONLY: it subtracts fragmentOffset into a local string variable and NEVER writes a local number back onto the SubHover (the GAP-2 invariant; the highlight keys the global auxMap)."
    - "The 'atom' card derives neighbours from sub.incidentPairs (the 'other endpoint' of each incident pair) — the field is populated by plan 02 at the LayerText construction site; this plan defines the type + consumer + tests against parsed-projection literals."
  prohibitions:
    - "No card body claims bond order, hydrogen count, or geometry (no 'single', 'double', 'order', 'geometry', or element words)."
    - "No reconstruction of the InChI string and no parser call inside subTokenInfo (verbatim-passthrough)."
    - "Never write a de-offset (local) number onto a SubHover field; de-offset is display-only."
---

<objective>
Fill the three `null` c-layer cases in the pure `subTokenInfo` module so a hovered/pinned
connection-layer atom/hyphen/parenthesis gets its own explanation card, and define the SubHover
type fields those cards consume. Test-first against real `getInchi()` fixtures.

Purpose: Phase 15 already highlights the c-layer precisely; Phase 17/18 built the card module but
left `'atom' | 'bond' | 'branch'` returning `null` (falling through to the generic "Connection
layer" card). This is the value core of v1.6 (CONN-01..04).

Output:
- `SubHover` gains `incidentPairs?` (atom neighbours, global) and `branchPoint?` (global) fields.
- `segmentBonds` exported (plan 02 reuses it at the LayerText atom-token site).
- Three new `subTokenInfo` cases + one `bondPairList` helper.
- The contradictory `subTokenInfo.test.ts:193-205` null-assertion block REPLACED, plus the new
  atom/bond/branch/component/copy-safety tests and the c-layer highlight-invariance guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/superpowers/specs/2026-06-29-c-layer-connection-cards-design.md
@.planning/phases/19-c-layer-connection-cards/19-RESEARCH.md
@.planning/phases/19-c-layer-connection-cards/19-VALIDATION.md
@src/lib/subTokenInfo.ts
@src/lib/parseInchi.ts
@src/lib/__tests__/subTokenInfo.test.ts
@src/lib/__tests__/highlightUtils.test.ts
</context>

<artifacts_this_phase_produces>
New symbols introduced by this plan (plan 02 wires them at the render site):
- `SubHover.incidentPairs?: [number, number][]` — the atom token's incident bond pairs, GLOBAL canonicals. The `'atom'` card derives its neighbour set from these.
- `SubHover.branchPoint?: number` — the branch-point atom (the atom the branch hangs off), GLOBAL canonical. The `'branch'` card names it (research open question A1 → explicit field, not direction inference).
- `segmentBonds` — promoted from internal to `export` so the LayerText atom-token site (plan 02) can compute incident pairs without writing a second adjacency walker.
- `bondPairList(pairs, off?)` — new helper in `subTokenInfo.ts` rendering `[[3,24],[24,11]]` as `3–24 and 24–11` (en-dash), de-offsetting each endpoint for display.
- `subTokenInfo` cases `'atom'`, `'bond'`, `'branch'` — replace the `default: return null`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 test scaffold — replace the null-fallthrough block and write the failing c-layer card tests (RED)</name>
  <files>src/lib/__tests__/subTokenInfo.test.ts, src/lib/__tests__/highlightUtils.test.ts</files>
  <read_first>
    - src/lib/__tests__/subTokenInfo.test.ts (the :193-205 "c-layer kinds fall through to null" describe block; the ALANINE and MELATONIN_TOLUENE fixture consts at :11-20; the GAP-1/GAP-2 hAtoms test precedents at :87-139)
    - .planning/phases/19-c-layer-connection-cards/19-VALIDATION.md (Per-Requirement Verification Map — every test name below comes from it)
    - docs/superpowers/specs/2026-06-29-c-layer-connection-cards-design.md (§Card copy — the exact body wording the tests assert against)
  </read_first>
  <behavior>
    Tests asserting the new contract (RED — they fail because subTokenInfo still returns null for c-layer kinds):
    - "atom card" (CONN-01): subTokenInfo({kind:'atom', canonical:2, incidentPairs:[[1,2],[2,3],[2,4]]}, {}) → title "Atom"; body contains "1, 3 and 4" (the deduped, sorted neighbour set of atom 2 in ALANINE's `c1-2(4)3(5)6`). Body contains "Atom 2".
    - "no bonds recorded" (CONN-01 empty-list guard / WR-04): subTokenInfo({kind:'atom', canonical:4, incidentPairs:[]}, {}) → body contains "no bonds recorded"; body does NOT contain "atoms  and" or "undefined".
    - "bond card" (CONN-02): subTokenInfo({kind:'bond', endpointPairs:[[1,2]]}, {}) → title "Bond"; body contains "1 and 2" (ALANINE `1-2`).
    - "branch card" (CONN-02): subTokenInfo({kind:'branch', bondPairs:[[2,4]], branchPoint:2}, {}) → title "Branch"; body names branch-point "atom 2" and bond pair "2–4" (ALANINE `(4)` off atom 2, en-dash).
    - "component" (CONN-03): a MELATONIN_TOLUENE component-2 (toluene, +17 offset) bond hover — subTokenInfo({kind:'bond', endpointPairs:[[18,22]], fragmentOffset:17, componentIndex:1}, {}) → body contains the per-component printed numbers "1 and 5" (18-17=1, 22-17=5, matching the printed `1-7-5-...`), body contains "(component 2)", body does NOT contain "18" or "22". (Derive the literal endpoint pair from MELATONIN_TOLUENE's component-2 c-segment `1-7-5-3-2-4-6-7` — the printed hyphen between 1 and 7… choose any real adjacent pair from that segment and assert its de-offset; the literal is the parsed projection of the real fixture, the hAtoms precedent.)
    - "copy safety" (CONN-04): for each of the atom/bond/branch cards above, body does NOT match /\b(single|double|triple|order|geometry|carbon|nitrogen|oxygen|hydrogen)\b/i. (Note: the atom/bond bodies legitimately say connectivity is "not bond order"; phrase the assertion so it forbids only POSITIVE claims — assert the body does not match /bond is (a |an )?(single|double|triple)/i and does not name an element word, rather than a bare grep for "order".)
    - "atom kind" / "bond kind" / "branch kind" (CONN-04): the REPLACED :193-205 block — assert subTokenInfo({kind:'atom'|'bond'|'branch', ...minimal valid fields}, {}) is NON-null (the exact reversal of the old contract).
    In highlightUtils.test.ts (CONN-03 highlight-invariance guard, mirror :803-820):
    - "GAP-2 guard" (atom kind): buildSubHoverSpecs({kind:'atom', canonical:19, fragmentOffset:17, componentIndex:1, incidentPairs:[[19,20]]}, {19:0}, atomElements, [], cLayer, makeMockStruct(), resolveVarFn) → specs.length === 1; specs[0].atoms contains the pool the GLOBAL canonical 19 maps to (NOT the de-offset 2, which would be auxMap[2] = undefined). Use the existing :803 hAtoms guard as the template for the auxMap/mock wiring.
    - "GAP-2 guard" (bond kind, for completeness): a bond-kind SubHover with non-zero fragmentOffset still resolves its bond via endpointPairs globals.
  </behavior>
  <action>
    Replace the entire `describe('subTokenInfo — c-layer kinds fall through to null', ...)` block at subTokenInfo.test.ts:193-205 with a new describe block asserting the NEW non-null contract (the "atom kind"/"bond kind"/"branch kind" non-null tests) — this is the Wave-0 requirement; leaving the old block makes the suite contradict itself. Then add the atom/bond/branch/component/copy-safety describe blocks per the behavior list. Reuse the existing ALANINE and MELATONIN_TOLUENE fixture consts (do NOT add new fixtures). Derive every SubHover literal (incidentPairs/endpointPairs/bondPairs/branchPoint/fragmentOffset) as the parsed projection of those real fixture strings — the exact legitimate precedent the file already uses for the hAtoms `[3,4,7,8,15]` literal (per a head comment, cite the real fixture each literal is projected from). In highlightUtils.test.ts, add the c-layer GAP-2 guard tests mirroring the hAtoms guard at :803-820 (same auxMap/atomElements/cLayer/makeMockStruct/resolveVarFn wiring; assert resolution via GLOBAL canonical). Run the tests and confirm they FAIL (RED) for the right reason (subTokenInfo returns null / cases not yet implemented) — NOT for a fixture or wiring error. Commit: `test(19-01): RED — c-layer connection card contract + highlight-invariance guard`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/subTokenInfo.test.ts src/lib/__tests__/highlightUtils.test.ts 2>&1 | grep -qE 'atom card|bond card|branch card' && echo "new tests present (expected RED at this step)"</automated>
  </verify>
  <done>The :193-205 null block is replaced by the non-null contract; the new atom/bond/branch/component/copy-safety tests and the c-layer GAP-2 guard exist and FAIL because the cases/guard data are not yet implemented (RED), with no fixture/wiring errors. Both fixture consts unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubHover type fields + segmentBonds export (parseInchi.ts)</name>
  <read_first>
    - src/lib/parseInchi.ts (the SubHover interface at :32-64 — see how endpointPairs/bondPairs/fragmentOffset/componentIndex are declared and commented; segmentBonds at :191-208; collectBranchPointBonds at :228-247)
  </read_first>
  <behavior>
    - The SubHover type compiles with two new optional fields and no consumer of the existing fields breaks (type-check is the gate; the existing 422 tests stay green).
    - segmentBonds is importable from './parseInchi' (plan 02 imports it; this plan only changes its visibility).
  </behavior>
  <action>
    In the SubHover interface, add two optional fields with comments matching the existing GAP-2/global-canonical doc style: `incidentPairs?: [number, number][]` — for the 'atom' kind, every [a,b] bond pair incident to the hovered atom, GLOBAL canonicals (the card derives neighbours as the other endpoint; never de-offset before storing); and `branchPoint?: number` — for the 'branch' kind, the GLOBAL canonical of the atom the branch hangs off (research open question A1: explicit field, not bondPairs[0][0] direction inference). Change `function segmentBonds(` to `export function segmentBonds(` so plan 02's LayerText atom-token site can reuse the existing adjacency walker (Don't-Hand-Roll: do NOT write a second adjacency parser). Do not change segmentBonds' body or signature. Run the full type-check + suite. Commit: `feat(19-01): SubHover incidentPairs/branchPoint fields + export segmentBonds`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/__tests__/parseInchi.test.ts</automated>
  </verify>
  <done>SubHover has `incidentPairs?: [number, number][]` and `branchPoint?: number`; `segmentBonds` is exported; `npx tsc --noEmit` passes and the parseInchi tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the three subTokenInfo c-layer cases + bondPairList helper (GREEN)</name>
  <read_first>
    - src/lib/subTokenInfo.ts (the `default: return null` at :97-99 to replace; the reused `atomList` at :36-41 and `componentMarker` at :110-113; `capitalise` at :103-105)
    - 19-RESEARCH.md §Code Examples (the three-case shape + bondPairList helper, lines 238-271) and §Pattern 2/3 (neighbour derivation + bond-pair list)
    - docs/superpowers/specs/2026-06-29-c-layer-connection-cards-design.md §Card copy (the exact body templates)
  </read_first>
  <behavior>
    The RED tests from Task 1 now pass (GREEN), specifically:
    - 'atom': neighbours = deduped, sorted "other endpoint" of each incidentPairs entry; de-offset for display via atomList(neighbours, off); zero-neighbour → "no bonds recorded"; one/many-neighbour grammar via atomList.
    - 'bond': reads the single canonical endpointPairs[0] (N* repeats it), de-offsets both endpoints, "Atoms a and b … bonded".
    - 'branch': branch-point = sub.branchPoint (fallback bondPairs[0]?.[0]); lists bond pairs via bondPairList(pairs, off).
    - All three append componentMarker(sub) and de-offset by sub.fragmentOffset for display ONLY — never mutate sub.
    - No body contains a positive bond-order/element/geometry claim.
  </behavior>
  <action>
    Replace the `default: return null` arm with the three explicit cases `case 'atom'`, `case 'bond'`, `case 'branch'` following 19-RESEARCH.md §Code Examples (lines 238-271). Add a module-level `bondPairList(pairs: [number, number][], off = 0): string` helper next to `atomList` (Pattern 3): formats each pair as `${a-off}–${b-off}` with an en-dash, joined with comma + "and" grammar matching atomList. For 'atom': derive `neighbours = [...new Set((sub.incidentPairs ?? []).map(([a,b]) => a === sub.canonical ? b : a))].sort((x,y)=>x-y)`; empty → the "no bonds recorded" body; else `atomList(neighbours, off)`. De-offset `selfLocal = sub.canonical! - off` for the "Atom N" phrasing. For 'branch': `bp = sub.branchPoint ?? sub.bondPairs?.[0]?.[0] ?? 0`. Use the exact connectivity-only caveat clause from the design ("records connectivity only, not bond order, hydrogens, or 3-D shape") — phrased as a NEGATIVE (what the layer does NOT encode), so the copy-safety test's positive-claim assertion passes. Reuse `atomList`/`componentMarker`/`capitalise` verbatim; do NOT read any string or call a parser. Run the c-layer test subset, then the full suite. Commit: `feat(19-01): GREEN — atom/bond/branch connection cards + bondPairList`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/subTokenInfo.test.ts src/lib/__tests__/highlightUtils.test.ts</automated>
  </verify>
  <done>All Task-1 tests pass (GREEN): atom card enumerates the full neighbour set, empty list says "no bonds recorded" with no "undefined"/"atoms  and", bond names two atoms, branch names branch-point + bond pairs, component-2 cards de-offset + carry "(component 2)" and never show global numbers, copy-safety holds (no positive bond-order/element/geometry claim), the highlight-invariance guard resolves via GLOBAL canonical. `npx vitest run` (full suite) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM → app | InChI string is computed locally by the ketcher-standalone WASM worker, not user-supplied free text. The parsed SubHover numeric fields cross into the pure card module. |
| card string → DOM | subTokenInfo returns plain strings; Phase 18's existing render path emits them as React text children. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Tampering / XSS | c-layer card prose (subTokenInfo 'atom'/'bond'/'branch' bodies) | low | mitigate | Bodies contain only digits, en-dashes, and fixed copy assembled from already-offset numeric SubHover fields. Rendered as React text children by the pre-existing Phase 18 path — NO `dangerouslySetInnerHTML` is added (this plan touches no React file). Verbatim-passthrough: no string reconstruction, no parser call in subTokenInfo. |
| T-19-02 | Information disclosure | display-only de-offset | low | accept | Cards render only WASM-derived parsed data (atom numbers the user already sees in the InChI string + on canvas). No secrets, no PII, no network. Read-only pure function. |
| T-19-03 | Tampering / state corruption | GAP-2 highlight invariant | low | mitigate | De-offset is display-only inside subTokenInfo (into a local string variable), NEVER written back onto the SubHover; SubHover payloads stay global canonicals (the auxMap key). Enforced by the buildSubHoverSpecs highlight-invariance guard test (CONN-03). |

> No package installs in this phase — no `T-19-SC` supply-chain item (Package Legitimacy Audit is N/A per 19-RESEARCH.md). No schema/ORM files in scope.
</threat_model>

<verification>
- `npx vitest run` — full suite green (422 baseline + the new c-layer tests).
- `npx tsc --noEmit` — type-check passes with the two new SubHover fields.
- CONN-01: atom card lists the full neighbour set (ALANINE atom 2 → "1, 3 and 4"); empty list → "no bonds recorded", no "undefined".
- CONN-02: bond card names the two joined atoms; branch card names branch-point + bond pairs (en-dash).
- CONN-03: component-2 card de-offsets to printed numbers + "(component 2)"; highlight-invariance guard resolves via GLOBAL canonical.
- CONN-04: copy-safety — no positive bond-order/element/geometry claim; pure module; real fixtures; empty-list guard.
</verification>

<success_criteria>
- The three c-layer `subTokenInfo` cases return cards (no longer null); `subTokenInfo.test.ts:193-205` replaced.
- `SubHover` carries `incidentPairs?` and `branchPoint?`; `segmentBonds` exported.
- All four CONN requirements have a passing automated test; full suite + type-check green.
- No de-offset number is ever written onto a SubHover (display-only).
</success_criteria>

<output>
Create `.planning/phases/19-c-layer-connection-cards/19-01-SUMMARY.md` when done.
</output>
