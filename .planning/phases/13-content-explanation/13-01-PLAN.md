---
phase: 13-content-explanation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/inchiKeyInfo.ts
  - src/lib/__tests__/inchiKeyInfo.test.ts
  - src/components/Explanation.tsx
autonomous: true
requirements: [INKEY-07, INKEY-08, INKEY-09, INKEY-10, INKEY-11, INKEY-12]

must_haves:
  truths:
    - "D-01: A chemist hovering any InChIKey segment sees what that block encodes plus the one-way-hash lesson — content lives only inside the existing per-segment hover cards, with no new standing caption or idle key card added"
    - "D-02: Every hover card carries the shared tagline: the key is a one-way hash, not decodable, not atom-mapped, which is why segments don't highlight"
    - "D-03/D-04: The skeleton card explains the 14-char connectivity hash, the 27-char DB-search purpose, the same-connectivity→same-first-block lookup basis, and one-key-per-assembly"
    - "D-03: The hash card explains the 8-char remaining-layers hash (stereo/isotope/proton) and the collision caveat"
    - "D-03: The flagVersion card explains S=standard / N=non-standard and A=version-1"
    - "D-03: The protonation card explains the single protonation char"
    - "D-05: Each card body is 1–3 tight chemist-voice sentences plus the shared tagline, matching LAYER_INFO terseness"
    - "D-06: Key cards show title + blurb only — no 'Reads as'/decode line and no echoed live slice value"
    - "D-07: No prose implies the key is reversible, atom-mappable, or a guaranteed unique identifier"
    - "D-08: No code reconstructs or re-joins the key from segments — this phase touches prose data only"
    - "A unit test pins each zone's label and the segment offsets it describes so prose cannot drift from parseInchiKey.ts"
  artifacts:
    - path: src/lib/inchiKeyInfo.ts
      provides: "KEY_ZONE_COPY data — label/title/body for all 4 zones; SHARED_TAGLINE constant"
      exports: [KEY_ZONE_COPY, SHARED_TAGLINE]
    - path: src/lib/__tests__/inchiKeyInfo.test.ts
      provides: "SC-1 offset/label pinning test"
      contains: "skeleton 0–14, hash 15–23, flag 23–24, version 24–25, protonation 26–27"
    - path: src/components/Explanation.tsx
      provides: "Updated KEY_ZONE_COPY import — delegates to inchiKeyInfo.ts"
  key_links:
    - from: src/components/Explanation.tsx
      to: src/lib/inchiKeyInfo.ts
      via: "named import KEY_ZONE_COPY"
      pattern: "import.*KEY_ZONE_COPY.*inchiKeyInfo"
    - from: src/lib/__tests__/inchiKeyInfo.test.ts
      to: src/lib/parseInchiKey.ts
      via: "imported parseInchiKey for offset cross-check"
      pattern: "parseInchiKey"
---

<objective>
Author the InChIKey explanation prose for all 4 hover zones (skeleton / hash / flagVersion / protonation), extract it into a testable `src/lib/inchiKeyInfo.ts` module, wire Explanation.tsx to import from it, and add the SC-1 unit test that pins each zone's label against the segment offsets from parseInchiKey.ts.

Purpose: Phase 12 built the card surface and hover wiring; Phase 13 replaces the placeholder body text with accurate, pedagogically correct content. The central teaching point — that the InChIKey is a one-way hash with no atom mapping, which is why its segments never highlight — must be reachable from any segment hover.

Output: src/lib/inchiKeyInfo.ts, src/lib/__tests__/inchiKeyInfo.test.ts, updated src/components/Explanation.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-content-explanation/13-CONTEXT.md

<!-- Voice/shape model to mirror: -->
@src/lib/layerInfo.ts

<!-- Segment offsets — source of truth for SC-1 test: -->
@src/lib/parseInchiKey.ts

<!-- The render site — KEY_ZONE_COPY consumer and key-card branch: -->
@src/components/Explanation.tsx

<!-- Companion test model to mirror structurally: -->
@src/lib/__tests__/layerInfo.test.ts

<!-- Zone-to-segment mapping confirming 4-card structure: -->
@src/components/InchiKeySection.tsx
</context>

<interfaces>
<!-- Existing KEY_ZONE_COPY shape in Explanation.tsx (lines 27–32) — the type to preserve: -->

From src/components/Explanation.tsx:
  KEY_ZONE_COPY: Record<KeyHoverZone, { label: string; title: string; body: string }>
  KeyHoverZone = 'skeleton' | 'hash' | 'flagVersion' | 'protonation'  (from src/store)

<!-- Segment offsets from parseInchiKey.ts (lines 60–66) — must be cited in SC-1 assertions: -->
  skeleton:    start: 0,  end: 14   (14 chars)
  hash:        start: 15, end: 23   (8 chars)
  flag:        start: 23, end: 24   (1 char)
  version:     start: 24, end: 25   (1 char)
  protonation: start: 26, end: 27   (1 char)

<!-- LAYER_INFO entry shape to mirror for voice/terseness (layerInfo.ts lines 13–18): -->
  interface LayerInfoEntry { title: string; accent: string; blurb: string; egLabel: string; eg?: string; }
  — KEY_ZONE_COPY uses { label, title, body }; no egLabel/eg/accent needed (key cards have no "Reads as" line per D-06)
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extract inchiKeyInfo.ts and author prose for all 4 zones</name>
  <files>src/lib/inchiKeyInfo.ts, src/components/Explanation.tsx</files>

  <read_first>
    src/lib/layerInfo.ts — voice model: LAYER_INFO entries show the terse 1–3-sentence chemist register to match.
    src/lib/parseInchiKey.ts — offset table to make concrete prose references accurate.
    src/components/Explanation.tsx — current KEY_ZONE_COPY shape and the key-card render branch; import site to update.
    .planning/phases/13-content-explanation/13-CONTEXT.md — locked decisions D-01 through D-08 and specifics.
  </read_first>

  <action>
    Create src/lib/inchiKeyInfo.ts. Export a string constant SHARED_TAGLINE containing the one-way-hash gist (per D-02): state that the InChIKey is a one-way hash — it cannot be decoded back to the structure or mapped to atoms, which is why these segments do not highlight. Exact wording is discretionary; the meaning is mandatory.

    Export KEY_ZONE_COPY typed as Record matching the { label: string; title: string; body: string } shape expected by Explanation.tsx. Populate all 4 zones as follows — the body of each zone must end with SHARED_TAGLINE (or incorporate its full gist inline):

    skeleton zone (INKEY-07, INKEY-08, INKEY-11, D-03, D-04):
      label: 'skeleton hash'
      title: 'Skeleton hash'
      body: State that this is the 14-character connectivity/skeleton hash. Add the purpose statement: the InChIKey is a fixed 27-character, web- and database-search-friendly hashed form of the InChI. State that molecules sharing the same connectivity share this first block — the basis for InChIKey database and web lookup. State that a multi-component or salt structure yields one key for the whole drawn assembly, not per-fragment keys (D-04). Close with SHARED_TAGLINE gist.

    hash zone (INKEY-07, INKEY-10, D-03):
      label: 'remaining-layers hash'
      title: 'Remaining-layers hash'
      body: State that this is the 8-character hash of the remaining InChI layers — stereo, isotope, and proton information. Include the collision caveat (INKEY-10): collisions are improbable but theoretically possible, so the key is for lookup and indexing, not proof of identity. Close with SHARED_TAGLINE gist.

    flagVersion zone (INKEY-07, INKEY-12, D-03):
      label: 'flag + version'
      title: 'Standard flag & version'
      body: State that S means standard InChI and N means non-standard InChI (INKEY-12). State that the second character A identifies InChIKey version 1. Close with SHARED_TAGLINE gist.

    protonation zone (INKEY-07, D-03):
      label: 'protonation'
      title: 'Protonation flag'
      body: State that this single character encodes the protonation state of the drawn assembly (N = neutral is the standard/neutral preset). Close with SHARED_TAGLINE gist.

    Voice constraints (D-05, D-07): Each body must be 1–3 tight sentences plus the shared tagline. No prose may use the words "reverse", "decode" (in a sense implying reversibility), "unique identifier", or any phrasing that implies the key can be used to recover the structure or is a guaranteed unique identity. The key is for lookup/indexing, never proof of identity.

    After creating inchiKeyInfo.ts, update src/components/Explanation.tsx:
      - Remove the inline KEY_ZONE_COPY declaration (lines 27–32).
      - Add a named import of KEY_ZONE_COPY from '../lib/inchiKeyInfo'.
      - Leave all other code in Explanation.tsx exactly as-is: no store fields, no render changes, no new UI surface, no dangerouslySetInnerHTML in the key branch, no canvas/highlight wiring.
      - Do NOT add any SHARED_TAGLINE import to Explanation.tsx; it is only needed by the test.
  </action>

  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npm test -- --run src/lib/__tests__/layerInfo.test.ts 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - src/lib/inchiKeyInfo.ts exists and exports KEY_ZONE_COPY and SHARED_TAGLINE.
    - KEY_ZONE_COPY.skeleton.body contains "14" and "27" (the char counts from INKEY-07/08).
    - KEY_ZONE_COPY.skeleton.body contains text conveying same-connectivity→same-first-block (INKEY-11).
    - KEY_ZONE_COPY.skeleton.body contains text conveying one-key-per-assembly (D-04).
    - KEY_ZONE_COPY.hash.body contains "8" (the char count from INKEY-07).
    - KEY_ZONE_COPY.hash.body contains text conveying the collision caveat: improbable but possible, for lookup/indexing not proof of identity (INKEY-10).
    - KEY_ZONE_COPY.flagVersion.body contains "S" and "N" (INKEY-12).
    - KEY_ZONE_COPY.flagVersion.body contains "A" and "version" (INKEY-12).
    - Every zone body contains the one-way-hash tagline gist (no decode/no atom mapping/why no highlight) — verified by grep or string.includes on the SHARED_TAGLINE or equivalent prose.
    - No zone body contains "reverse" or "unique identifier" as a reversal/identity claim.
    - src/components/Explanation.tsx imports KEY_ZONE_COPY from '../lib/inchiKeyInfo' (not declared inline).
    - Existing layerInfo tests remain green: npm test -- --run src/lib/__tests__/layerInfo.test.ts exits 0.
  </acceptance_criteria>

  <done>inchiKeyInfo.ts ships all 4 zones with accurate, D-05-terse, D-07-compliant prose; Explanation.tsx imports it; all pre-existing tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SC-1 offset/label unit test for inchiKeyInfo</name>
  <files>src/lib/__tests__/inchiKeyInfo.test.ts</files>

  <read_first>
    src/lib/__tests__/layerInfo.test.ts — structural model for the companion test (describe blocks, assertion style).
    src/lib/parseInchiKey.ts — the segment offset table that is the ground truth (skeleton 0–14, hash 15–23, flag 23–24, version 24–25, protonation 26–27).
    src/lib/inchiKeyInfo.ts — KEY_ZONE_COPY and SHARED_TAGLINE to assert against.
    .planning/phases/13-content-explanation/13-CONTEXT.md — SC-1 specification.
  </read_first>

  <action>
    Create src/lib/__tests__/inchiKeyInfo.test.ts as a Vitest test file mirroring the structure of layerInfo.test.ts.

    Import KEY_ZONE_COPY and SHARED_TAGLINE from '../inchiKeyInfo'. Import parseInchiKey from '../parseInchiKey'.

    Write the following describe/it blocks:

    describe('KEY_ZONE_COPY — all zones present'):
      Assert KEY_ZONE_COPY has entries for all four keys: 'skeleton', 'hash', 'flagVersion', 'protonation'.
      Assert each entry has non-empty label, title, and body strings.

    describe('SC-1 — zone labels map to correct segment offsets'):
      Declare a reference key for offset cross-checking: use the standard/neutral InChIKey pattern 'AAAAAAAAAAAAAA-BBBBBBBBSA-N' (a 27-char conforming string where the flag char at position 23 is 'S' and version at 24 is 'A' and protonation at 26 is 'N'; the exact letter values don't matter for offset assertions, but using 'S'/'A'/'N' in the right positions makes the test self-documenting).
      Call parseInchiKey on that reference key and assert it returns 5 segments.
      For each parsed segment, assert the start/end values match the canonical offsets:
        skeleton: start 0, end 14
        hash: start 15, end 23
        flag: start 23, end 24
        version: start 24, end 25
        protonation: start 26, end 27
      Assert that the 'skeleton' zone label from KEY_ZONE_COPY.skeleton.label contains "skeleton" (case-insensitive).
      Assert that the 'hash' zone label from KEY_ZONE_COPY.hash.label contains "hash" (case-insensitive).
      Assert that KEY_ZONE_COPY.flagVersion.label contains "flag" or "version" (case-insensitive).
      Assert that KEY_ZONE_COPY.protonation.label contains "protonation" (case-insensitive).

    describe('SC-1 — block-size consistency: zone body cites correct char counts'):
      Assert KEY_ZONE_COPY.skeleton.body includes '14' (references the 14-char skeleton hash, INKEY-07).
      Assert KEY_ZONE_COPY.skeleton.body includes '27' (references the 27-char total key, INKEY-08).
      Assert KEY_ZONE_COPY.hash.body includes '8' (references the 8-char hash block, INKEY-07).

    describe('SC-1 — SHARED_TAGLINE present on every zone (INKEY-09 / D-02)'):
      For each of the 4 zone keys, assert that the body string includes SHARED_TAGLINE
      OR assert that it includes a substring from SHARED_TAGLINE that encodes the core one-way-hash gist.
      Use a conservative sub-string check: SHARED_TAGLINE.slice(0, 20) is sufficient to pin the constant, or assert body.length > 40 and body === body (trivial guard is too weak; prefer asserting the tagline text appears verbatim or that both 'hash' and 'atom' or 'highlight' appear in the body).

    describe('D-07 — no reversibility/identity claims'):
      For each zone body, assert the string does NOT include 'reverse' (covers "reversible").
      For each zone body, assert it does NOT match /unique identifier/i.

    Use `it` (not `test`) to mirror the existing test style. No snapshot tests. All assertions must be deterministic.
  </action>

  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npm test -- --run src/lib/__tests__/inchiKeyInfo.test.ts 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - src/lib/__tests__/inchiKeyInfo.test.ts exists and contains at least 12 `it(` assertions across the described blocks.
    - npm test -- --run src/lib/__tests__/inchiKeyInfo.test.ts exits 0 with all tests passing.
    - The test file imports and calls parseInchiKey, asserting the canonical offsets (skeleton 0–14, hash 15–23, flag 23–24, version 24–25, protonation 26–27).
    - The test file asserts each zone body includes the expected char-count references ('14', '27', '8').
    - The test file asserts each zone body includes the one-way-hash gist (SHARED_TAGLINE or sub-string check).
    - The test file asserts no zone body contains 'reverse' or 'unique identifier'.
    - Full test suite passes: npm test exits 0 (no regressions in layerInfo.test.ts or any other test).
  </acceptance_criteria>

  <done>SC-1 test is green; parseInchiKey offsets are pinned; prose correctness invariants are encoded as assertions; full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None — content-only phase | This phase edits static string constants (prose). No new inputs cross any trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | KEY_ZONE_COPY body strings | accept | Prose is static plain-text data compiled into the bundle. No user input, no network, no dynamic construction. The React render branch already uses text children (not dangerouslySetInnerHTML) for key cards — established in Phase 12. No new attack surface is introduced. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new packages are installed in this phase. inchiKeyInfo.ts is a pure data module; its test uses Vitest (already installed). Zero supply-chain exposure added. |

No new XSS surface: prose is passed as React text children in the existing key-card branch (`<p className={styles.layerBody}>{KEY_ZONE_COPY[keyHoverKind].body}</p>`). JSX text children are escaped by React. dangerouslySetInnerHTML is not used in this branch. No HTML markup may appear in body strings.
</threat_model>

<verification>
Run the full test suite after both tasks:

  cd /home/bsmue/code/explain-that-inchi && npm test -- --run

Expected: all existing tests plus the new inchiKeyInfo.test.ts pass. Zero failures.

Spot-check prose correctness (manual read after Task 1):
  - Open src/lib/inchiKeyInfo.ts and read each body string aloud.
  - Confirm SHARED_TAGLINE or equivalent one-way-hash gist ends each body.
  - Confirm no body uses "reverse", "unique identifier", or implies atom-mapping.
  - Confirm skeleton body cites "14", "27", lookup basis, one-key-per-assembly.
  - Confirm hash body cites "8" and the collision caveat.
  - Confirm flagVersion body names S, N, A, and version 1.
</verification>

<success_criteria>
- npm test exits 0 with full suite green (pre-existing + new inchiKeyInfo tests).
- KEY_ZONE_COPY in inchiKeyInfo.ts has 4 zones; each body is 1–3 sentences plus shared tagline.
- All 6 requirement IDs are covered: INKEY-07 (block structure in all 4 cards), INKEY-08 (27-char purpose on skeleton), INKEY-09 (one-way hash / no atom map / no highlight tagline on every card), INKEY-10 (collision caveat on hash), INKEY-11 (same-connectivity→same-first-block on skeleton), INKEY-12 (S/N/A on flagVersion).
- Explanation.tsx imports KEY_ZONE_COPY from inchiKeyInfo.ts — no inline prose data remains.
- No new UI surface, store field, canvas behavior, or highlight wiring introduced.
- No prose implies reversibility, atom-mapping, or guaranteed uniqueness (D-07).
</success_criteria>

<output>
Create .planning/phases/13-content-explanation/13-01-SUMMARY.md when done.
</output>
