---
context: phase
phase: 17-sub-token-copy-core-element-table-pure-module-tests
task: 3
total_tasks: 3
status: paused
last_updated: 2026-06-26T13:24:36.388Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Out-of-workflow source edits | A subagent removed a dead `ELEMENT_NAMES` import from `subTokenInfo.test.ts` in the working tree, outside any GSD task. It was reverted to keep committed code == verified code. | advisory | CLAUDE.md rule: no Edit/Write outside a GSD command. Verifier/reviewer agents must not mutate source. |

<current_state>
Phase 17 is fully implemented, committed, and green on a clean tree. Both plans are done
(SUMMARYs written). Automated verification passed 5/5 must-haves; full suite 401/401, `tsc`
clean, `vite build` clean. Verification status is `human_needed` ONLY because the SUBEX-10
chemist-prose accuracy gate is a judgment call — and that gate has been folded into Phase 18.
Phase 17's ROADMAP checkbox stays unchecked until Phase 18's live chemist review passes.
</current_state>

<completed_work>

- 17-01: Extended `ELEMENT_NAMES` in `src/lib/layerInfo.ts` 10→120 entries (118 IUPAC H–Og + D/T),
  lowercase IUPAC-2005 spelling, in place; D-19 invariants pinned in `layerInfo.test.ts`. (commit 9ab2422)
- 17-02: New pure `src/lib/subTokenInfo.ts` — `subTokenInfo(sub, atomElements)` → card copy for
  element/hAtoms/mobileH/stereo, null for atom/bond/branch; real-fixture TDD suite
  (`subTokenInfo.test.ts`) with alanine + user-confirmed methylamine·HCl salt
  (`InChI=1S/CH5N.ClH/c1-2;/h2H2,1H3;1H`). Verbatim-passthrough holds. (commit 6d3e861)
- Code review (advisory): 17-REVIEW.md — 0 critical, 4 warning, 3 info. (commit caa84f4)
- Verification: 17-VERIFICATION.md, human_needed, 5/5. (commit ca37116)
- UAT persisted: 17-UAT.md (1 pending item, deferred to Phase 18). (commit f07b3f5)
</completed_work>

<remaining_work>

- None within Phase 17's code scope.
- The single human item (SUBEX-10 chemist-prose accuracy) is folded into Phase 18's live chemist gate.
</remaining_work>

<decisions_made>

- Fold Phase 17's SUBEX-10 chemist-prose verify item into Phase 18's live chemist review
  (Phase 18 SC#5 already owns it; cards only render live in Phase 18). Do NOT run
  `/gsd-verify-work 17` separately.
- Reverted an unsanctioned subagent edit to keep committed code identical to verified code.
</decisions_made>

<blockers>
- None. Phase 17 code is complete and green; the only "open" item is the Phase-18-folded chemist gate.
</blockers>

## Required Reading (in order)
1. `.planning/phases/17-.../17-VERIFICATION.md` — what passed and the one deferred item.
2. `.planning/phases/17-.../17-REVIEW.md` — advisory findings; WR-01/WR-02 are real edge-case guards worth landing before Phase 18 wires real `SubHover` construction.
3. `.planning/phases/17-.../17-02-PLAN.md` + `17-CONTEXT.md` — the D-04..D-18 copy rules `subTokenInfo` implements (Phase 18 wires this into the card).

## Infrastructure State
- No running services. WASM InChI generation is browser-only (`npm run dev` → http://localhost:5173) — relevant for Phase 18's live chemist gate.
- Test command is `npx vitest run` (NOT `npm test`, which is vitest watch mode and hangs).

<context>
Phase 17 (v1.5) is the pure value core. `subTokenInfo` is the single source of sub-token card
prose; Phase 18 wires one precedence branch into the existing Explanation card so hover/pin
updates it, then verifies the live strings against real molecules with a chemist. The two open
advisory bugs (WR-01 empty `atoms[]` → "Infinity–-Infinity"; WR-02 `sub.el!` throws if `el`
missing) are not reachable from valid InChI today but become reachable once Phase 18 constructs
real `SubHover` values — fix them as guards early in Phase 18.
</context>

<next_action>
Start with: `/gsd-discuss-phase 18` (or `/gsd-plan-phase 18` if ready). Phase 18 owns the chemist
gate that completes Phase 17's verification. Consider landing 17-REVIEW.md WR-01/WR-02 guards first.
</next_action>
