---
phase: 17-sub-token-copy-core-element-table-pure-module-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/layerInfo.ts
  - src/lib/__tests__/layerInfo.test.ts
autonomous: true
requirements: [SUBEX-08]
must_haves:
  truths:
    - "ELEMENT_NAMES resolves a non-organic symbol (K -> potassium) and stays case-exact (Co -> cobalt, never confused with C+O)"
    - "ELEMENT_NAMES has exactly 120 entries (118 IUPAC H-Og + D + T)"
    - "Existing layerInfo/formulaReading/elementColor tests stay green after the table extension"
  artifacts:
    - src/lib/layerInfo.ts
    - src/lib/__tests__/layerInfo.test.ts
  key_links:
    - "formulaSegmentReading reads ELEMENT_NAMES[el] || el — additive extension must not change single-fragment output"
    - "elementColor keeps its own independent 10-element whitelist — table extension must NOT widen the colour palette"
---

<objective>
Extend the existing `ELEMENT_NAMES` table in `src/lib/layerInfo.ts` from its 10 organic
entries to the full periodic table — 120 entries (118 IUPAC elements H–Og + the `D`/`T`
pseudo-symbols) — in place, with IUPAC-2005 spelling and lowercase values, so no element
ever surfaces as a bare symbol downstream. Pin the result with D-19 assertions in the
existing `layerInfo.test.ts`. Satisfies SUBEX-08.

Purpose: SUBEX-08 backs the Phase-17 element card (and Phase-18 element-hover behaviour)
with a complete symbol→name lookup. This plan is the prerequisite table for Plan 02's
`subTokenInfo` module, which imports `ELEMENT_NAMES`.

Output: An extended `ELEMENT_NAMES` (120 entries, same export signature) and new D-19 test
assertions; existing `layerInfo`/`formulaReading`/`elementColor` tests remain green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-sub-token-copy-core-element-table-pure-module-tests/17-CONTEXT.md
@.planning/phases/17-sub-token-copy-core-element-table-pure-module-tests/17-RESEARCH.md
@.planning/phases/17-sub-token-copy-core-element-table-pure-module-tests/17-PATTERNS.md

# Files this plan modifies
@src/lib/layerInfo.ts
@src/lib/__tests__/layerInfo.test.ts
</context>

<artifacts_this_phase_produces>
This plan (17-01) produces:
- `ELEMENT_NAMES` (existing export in `src/lib/layerInfo.ts`) extended in place to 120 entries — NO signature change (`Record<string, string>`), values lowercase.
- New D-19 `describe` block in `src/lib/__tests__/layerInfo.test.ts` (no new file).

Plan 17-02 (separate plan) produces the new `src/lib/subTokenInfo.ts` module, its
`subTokenInfo` function, the `SubTokenCopy` return type, and the new
`src/lib/__tests__/subTokenInfo.test.ts`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Extend ELEMENT_NAMES to 120 entries in place</name>
  <read_first>
    - src/lib/layerInfo.ts (lines 142–192 — the `ELEMENT_NAMES` literal at 180–183, plus its two in-file readers `formulaSegmentReading` at 142–154 and `elementColor` at 189–192)
    - .planning/phases/17-sub-token-copy-core-element-table-pure-module-tests/17-PATTERNS.md § "src/lib/layerInfo.ts — ELEMENT_NAMES extension" (analog: itself, additive, in place)
    - .planning/phases/17-sub-token-copy-core-element-table-pure-module-tests/17-RESEARCH.md § "State of the Art" (IUPAC-2005 spelling notes: keep `sulfur`/`phosphorus`; use `aluminium`, `caesium`)
  </read_first>
  <action>
    Extend the existing `ELEMENT_NAMES` literal in `src/lib/layerInfo.ts` (currently the 10
    organic entries `H, C, N, O, S, P, F, Cl, Br, I` at lines 180–183) in place to exactly
    120 entries per D-01: all 118 IUPAC elements in atomic-number order (H=1 through Og=118),
    plus the two pseudo-symbols `D` (deuterium) and `T` (tritium) the i-layer/formula can
    carry (D-01). Keep the export signature `Record<string, string>` and values lowercase
    (D-02/Pitfall-4 — capitalisation happens at the title site in Plan 02, never in the
    table, so `formulaSegmentReading` stays byte-identical). Use IUPAC-2005 spelling per D-02:
    retain the existing `S: 'sulfur'` and `P: 'phosphorus'`; use `aluminium` (Al), `caesium`
    (Cs); element 118 is `oganesson`; 113–117 are nihonium/moscovium/tennessine/livermorium
    and flerovium (114) per the RESEARCH § State of the Art / IUPAC list. Symbol keys are
    case-exact two-letter forms (`Co`, `Cl`, `Na`, etc.) — D-03; never `.toUpperCase()`.
    Do NOT touch `elementColor` (lines 189–192): it keeps its own independent 10-element
    whitelist — extending the name table must NOT widen the colour palette (PATTERNS § in-file
    consumer 2). Do NOT change `formulaSegmentReading`, `subscript`, or any other export.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/layerInfo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/layerInfo.test.ts` exits 0 (existing tests stay green — additive extension cannot break `formulaReading`/`elementColor`/`subscript`)
    - `Object.keys(ELEMENT_NAMES).length === 120`
    - Values are lowercase; `S` is still `'sulfur'`, `P` is still `'phosphorus'`, `Al` is `'aluminium'`, `Cs` is `'caesium'`
    - `elementColor` body unchanged (still a 10-element whitelist)
  </acceptance_criteria>
  <done>
    `ELEMENT_NAMES` contains exactly 120 lowercase IUPAC-2005-spelled entries (118 elements +
    D + T), same export signature; `formulaSegmentReading` and `elementColor` are byte-unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Pin D-19 element-table invariants in layerInfo.test.ts</name>
  <read_first>
    - src/lib/__tests__/layerInfo.test.ts (lines 1–45 — existing imports + describe layout; add `ELEMENT_NAMES` to the import from `../layerInfo`)
    - .planning/phases/17-sub-token-copy-core-element-table-pure-module-tests/17-PATTERNS.md § "src/lib/__tests__/layerInfo.test.ts (MODIFY — add D-19 pins)" (the exact assertion list)
    - .planning/phases/17-sub-token-copy-core-element-table-pure-module-tests/17-RESEARCH.md § "Invariants Pinned by Assertion" (the D-19 pins)
  </read_first>
  <action>
    Add a new `describe('ELEMENT_NAMES — D-19 full periodic table', ...)` block to
    `src/lib/__tests__/layerInfo.test.ts`, importing `ELEMENT_NAMES` from `../layerInfo`
    alongside the existing imports. Pin all of D-19 (RESEARCH § Invariants Pinned by Assertion):
    `Object.keys(ELEMENT_NAMES).length` toBe 120; case-exactness via `ELEMENT_NAMES['Co']`
    toBe `'cobalt'` AND `ELEMENT_NAMES['C']` toBe `'carbon'` AND `ELEMENT_NAMES['O']` toBe
    `'oxygen'` (proving `Co` is not silently treated as `C`+`O`, D-03); a non-organic sample
    `ELEMENT_NAMES['K']` toBe `'potassium'`; and both pseudo-symbols present —
    `ELEMENT_NAMES['D']` and `ELEMENT_NAMES['T']` toBeDefined. Leave every existing `describe`
    block in the file untouched (they must keep passing — D-19 "existing layerInfo tests stay green").
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/layerInfo.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/__tests__/layerInfo.test.ts` exits 0 with the new D-19 block passing
    - The new block asserts: length === 120; `Co`/`C`/`O` case-exact; `K` === potassium; `D` and `T` defined
    - All pre-existing `describe` blocks in the file still pass (untouched)
  </acceptance_criteria>
  <done>
    `layerInfo.test.ts` contains a passing D-19 block pinning count==120, case-exact `Co`/`C`/`O`,
    `K`=potassium, and `D`/`T` presence; the full file is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds static lookup data (element symbol→name strings) to a pure DOM-free library
module and pins it with unit-test assertions. There is **no new trust boundary**: no network,
no persistence, no user-controlled input, no DOM. The table is a hand-typed constant consumed
by pure string formatters. This is an honest "no new attack surface" assessment, not an absence
of analysis.

| Boundary | Description |
|----------|-------------|
| (none introduced) | Static constant data only; no input crosses any boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | `ELEMENT_NAMES` table correctness (mis-spelled/mis-counted entries silently mislabel an element) | low | mitigate | D-19 assertions pin count==120, case-exact `Co`/`C`/`O`, `K`=potassium, `D`/`T` present; the Phase-18 human chemist gate is the downstream content check |
| T-17-02 | Information Disclosure | Element-name strings rendered later as React text children (Phase 18) | low | accept | Strings are plain lowercase element names with no markup; rendered as text children in Phase 18, not `dangerouslySetInnerHTML` — no escaping risk introduced here |

No `high`/`critical` threats — no install tasks, no `[ASSUMED]`/`[SUS]` packages (this phase
installs nothing). No `T-{phase}-SC` supply-chain checkpoint required.
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/layerInfo.test.ts` exits 0 (D-19 block + all existing tests green)
- `npx vitest run` (full suite) green — confirms the extended table broke no other importer
</verification>

<success_criteria>
- `ELEMENT_NAMES` has exactly 120 entries; lookup is case-exact (`Co`→cobalt, `K`→potassium); `D`/`T` present
- Existing `layerInfo`/`formulaReading`/`elementColor`/`subscript` tests stay green
- Export signature unchanged (`Record<string, string>`); `elementColor` whitelist unchanged
</success_criteria>

<output>
Create `.planning/phases/17-sub-token-copy-core-element-table-pure-module-tests/17-01-SUMMARY.md` when done
</output>
