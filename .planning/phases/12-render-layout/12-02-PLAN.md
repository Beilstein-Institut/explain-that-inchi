---
phase: 12-render-layout
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/Explanation.tsx
autonomous: false
requirements: [INKEY-04]
must_haves:
  truths:
    - "D-03: Hovering an InChIKey segment surfaces a per-segment explanation card by extending the shared Explanation panel — no standalone card surface is created"
    - "The shared panel shows key-segment content when keyHoverKind is set, else InChI-layer content when hoverIdx is set, else idle (D-04a precedence)"
    - "D-07/D-08: The shared panel renders one combined card for the flag+version (flagVersion) zone — 4 key cards not 5 — while the InChIKey strip stays 5-segment granular"
    - "Hovering an InChIKey segment surfaces the card WITHOUT highlighting any atoms or bonds on the canvas"
    - "The existing InChI-layer card and idle card render unchanged when no key segment is hovered"
  artifacts:
    - path: "src/components/Explanation.tsx"
      provides: "keyHoverKind read + D-04a precedence branch rendering key-segment card scaffolding using existing card classes"
      contains: "keyHoverKind"
  key_links:
    - from: "src/components/Explanation.tsx"
      to: "src/store.ts"
      via: "useInchiStore(state => state.keyHoverKind)"
      pattern: "keyHoverKind"
---

<objective>
Extend the shared `Explanation` panel to render a per-segment card when an InChIKey segment is hovered, completing the hover-to-card half of INKEY-04. The card is wired to the `keyHoverKind` field (defined in 12-01), reusing the existing card CSS classes — no new card surface, no canvas highlighting. Phase 12 supplies minimal placeholder prose; Phase 13 fills the real content.

Purpose: Satisfies INKEY-04 (hovering a segment surfaces a per-segment explanation card mirroring the InChI strip). Preserves Invariant #2 — key hover reaches only this panel, never the canvas.
Output: `Explanation.tsx` reads `keyHoverKind`, applies D-04a precedence, renders 4 key-zone cards (skeleton / hash / flagVersion / protonation) reusing existing card markup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-render-layout/12-CONTEXT.md
@.planning/phases/12-render-layout/12-PATTERNS.md
@.planning/phases/12-render-layout/12-UI-SPEC.md

<interfaces>
<!-- Contracts from 12-01 (same wave-1 output) — use directly. -->

From src/store.ts (added in 12-01):
```typescript
export type KeyHoverZone = 'skeleton' | 'hash' | 'flagVersion' | 'protonation';
keyHoverKind: KeyHoverZone | null;
```

Existing Explanation read + precedence (src/components/Explanation.tsx lines 15-24):
```typescript
const layers = useInchiStore(state => state.layers);
const hoverIdx = useInchiStore(state => state.hoverIdx);
const layer = hoverIdx !== null ? layers[hoverIdx] : null;
const accentVar = layer ? `var(--c-${swatchVar(layer.type)})` : 'var(--ink-faint)';
```

Existing card classes (src/components/Explanation.module.css — reuse, do NOT add new): .card, .active, .layerTag, .swatch, .layerTitle, .layerBody, .layerEg, .lbl. Active card uses inline style `{ '--accent': accentVar }`.

Per-zone accent token (D-01): skeleton→--c-conn, hash→--c-stereo, flagVersion→--c-version, protonation→--c-proton.
</interfaces>

@src/components/Explanation.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read keyHoverKind and add D-04a precedence + key-segment card scaffolding</name>
  <files>src/components/Explanation.tsx</files>
  <read_first>
    - src/components/Explanation.tsx (the file being modified — current read at lines 15-24, idle branch lines 43-52, active branch lines 53-68, card markup classes)
    - src/store.ts (KeyHoverZone + keyHoverKind from 12-01)
    - src/components/Explanation.module.css (existing card classes to reuse: .card, .active, .layerTag, .swatch, .layerTitle, .layerBody)
    - .planning/phases/12-render-layout/12-CONTEXT.md § D-03, D-04, D-04a (extend the shared panel; key-hover read only; precedence)
    - .planning/phases/12-render-layout/12-PATTERNS.md § "src/components/Explanation.tsx" (scaffolding-only guidance + TEST-SAFETY GUARDRAIL)
  </read_first>
  <action>
    In src/components/Explanation.tsx add `const keyHoverKind = useInchiStore(state => state.keyHoverKind);` next to the existing selectors. Treat `undefined` the same as `null` (a `if (keyHoverKind)` check is falsy-safe — required because some store mocks omit the field).

    Implement D-04a precedence: when `keyHoverKind` is set, render the key-segment card; else when `layer` is set, render the existing InChI-layer card UNCHANGED; else render the existing idle card UNCHANGED. Do not alter the idle or InChI-layer branches or the `<Legend>` render.

    Key-segment branch (scaffolding only — Phase 13 supplies real prose): extend the SHARED Explanation panel (D-03 — no standalone card surface) by reusing the SAME card markup as the active-layer branch — `styles.card` + `styles.active`, with inline `style={{ '--accent': <zone accent> } as React.CSSProperties}`. Map the 4 zones (D-07/D-08: flag+version combine into the single flagVersion card) to accent tokens: skeleton→`var(--c-conn)`, hash→`var(--c-stereo)`, flagVersion→`var(--c-version)`, protonation→`var(--c-proton)` (D-01). Render `.layerTag` (with `.swatch` + a zone label, e.g. `skeleton`, `hash`, `flag + version`, `protonation`), `.layerTitle`, and `.layerBody` using minimal placeholder copy keyed by zone (a small const map of {title, body} per KeyHoverZone is fine — short, factual placeholders such as title `Skeleton hash` / body `First 14-character block of the InChIKey.`). Do NOT use `dangerouslySetInnerHTML` in this branch and do NOT add `.layerEg`/`readingFor` (no reading-code for key segments). Do NOT add any new CSS classes — reuse existing ones only.

    GUARDRAIL: this branch must read state only; it must NOT call setHover/setSubHover or otherwise reach useKetcherHighlights (Invariant #2 — the absence of canvas highlighting is the teaching point). The accent token is derived locally from keyHoverKind, not from swatchVar.
  </action>
  <verify>
    <automated>grep -q "keyHoverKind" src/components/Explanation.tsx &amp;&amp; ! grep -qE "setHover|setSubHover" src/components/Explanation.tsx &amp;&amp; [ "$(grep -c "dangerouslySetInnerHTML" src/components/Explanation.tsx)" = "1" ] &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npm test -- --run src/__tests__/InchiSection.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Explanation.tsx adds `const keyHoverKind = useInchiStore(state => state.keyHoverKind);`
    - D-03: the key-segment card is rendered by extending the shared Explanation panel — no standalone card surface / new component is introduced
    - Precedence is keyHoverKind → layer → idle (D-04a); idle and InChI-layer branches unchanged
    - D-07/D-08: exactly 4 key-zone cards exist (skeleton, hash, flagVersion combining flag+version, protonation) — the combined flagVersion card covers flag S/N + version A
    - Key-segment branch reuses `styles.card`/`styles.active`/`.layerTag`/`.swatch`/`.layerTitle`/`.layerBody` — no new CSS classes added to Explanation.module.css
    - Accent per zone (D-01): skeleton→--c-conn, hash→--c-stereo, flagVersion→--c-version, protonation→--c-proton, set via inline `--accent`
    - Explanation.tsx contains zero `setHover`/`setSubHover` calls (Invariant #2)
    - `dangerouslySetInnerHTML` count stays at exactly 1 (the existing InChI-layer branch); the key-segment branch adds none
    - `npx tsc --noEmit` exits 0
    - `src/__tests__/InchiSection.test.tsx` passes (SC-5) — the existing store mock omits keyHoverKind and the falsy-safe check must keep it green; if a test mock is extended it must add keyHoverKind + setKeyHoverKind
  </acceptance_criteria>
  <done>Explanation reads keyHoverKind, applies D-04a precedence, renders key-segment card scaffolding via existing classes; idle/layer branches and tests unchanged; no canvas highlighting path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify the full InChIKey strip end-to-end</name>
  <action>Pause for the developer to manually verify the rendered InChIKey strip in a running dev server per the steps below. This is a verification-only checkpoint — no code is written. Resume after the developer types "approved" or reports issues.</action>
  <what-built>
    The full InChIKey strip is now wired end-to-end: 12-01 rendered the colored segment strip + copy button + placeholder; this plan wired the shared Explanation panel to show a per-segment card on key hover. The Ketcher canvas, InChI strip, and existing tests are untouched.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app.
    2. Draw or load a preset molecule. Confirm an InChIKey strip appears BELOW the InChI strip, with 5 color-coded segments and two dimmed hyphens, led by a faint `InChIKey` prefix.
    3. Hover each segment zone (skeleton, hash, the combined flag+version pair, protonation). Confirm: (a) sibling segments dim to ~0.35 opacity; (b) the shared explanation panel shows a card for that segment; (c) NOTHING highlights on the molecule canvas (no atoms/bonds change color) — this is the central teaching point (Invariant #2).
    4. Confirm hovering an InChI layer (top strip) still highlights atoms as before, and the panel switches to the InChI-layer card.
    5. Click the InChIKey copy button — confirm 'Copied!' appears and resets after ~3s, and the pasted value equals the displayed key character-for-character.
    6. Clear the canvas (delete all atoms) — confirm the InChIKey strip shows the dimmed placeholder `Draw a molecule above to see its InChIKey.` and the canvas does NOT flicker/reload (WASM does not re-init).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (e.g. canvas highlights on key hover, wrong precedence, copy mismatch).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → Explanation panel (DOM) | `keyHoverKind` is an in-app enum value (one of 4 fixed strings) set by InchiKeySection; the card prose is static placeholder text authored in-repo. No network, no untrusted input. |

## STRIDE Threat Register

Pure client-side rendering of static, in-repo card text keyed by a 4-value enum. Minimal surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering (XSS) | Explanation key-segment card | mitigate | Key-segment card renders title/body as React text children (no `dangerouslySetInnerHTML` in this branch). React escapes text by default. The existing layer-branch `dangerouslySetInnerHTML` is unchanged and out of scope here. |
| T-12-05 | Elevation / invariant breach | keyHoverKind reaching the canvas | mitigate | Explanation reads `keyHoverKind` only and never calls setHover/setSubHover; useKetcherHighlights remains driven solely by hoverIdx/subHover (verified by grep gate + 12-01 Task 1). Preserves Invariant #2. |
| T-12-SC | Tampering | npm/pip/cargo installs | n/a | Zero new dependencies this plan. No install tasks → no package-legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `src/__tests__/InchiSection.test.tsx` passes (SC-5) with the falsy-safe keyHoverKind read.
- grep confirms the key-segment branch adds no setHover/setSubHover and no new dangerouslySetInnerHTML (Invariant #2).
- Human-verify checkpoint confirms: card appears on key hover, siblings dim, canvas stays un-highlighted, copy round-trips verbatim, placeholder on empty, no canvas remount.
</verification>

<success_criteria>
- INKEY-04: hovering an InChIKey segment surfaces a per-segment explanation card mirroring the InChI strip's hover behaviour. (SC-2)
- Local-state/zone hover surfaces the card WITHOUT highlighting canvas atoms/bonds. (SC-2 / Invariant #2)
- Shared panel precedence keyHoverKind → hoverIdx → idle (D-04a); existing branches unchanged.
- InChI strip + tests stay green (SC-5).
</success_criteria>

<output>
Create `.planning/phases/12-render-layout/12-02-SUMMARY.md` when done.
</output>
