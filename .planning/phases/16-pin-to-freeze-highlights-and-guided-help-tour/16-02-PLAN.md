---
phase: 16-pin-to-freeze-highlights-and-guided-help-tour
plan: 02
type: execute
wave: 1
depends_on: []  # build tasks (Help button/overlay/wiring/Caffeine auto-load) are independent of 16-01; only the human-verify step-6 pin test exercises 16-01's pin feature, gated below to run after BOTH wave-1 plans merge
files_modified:
  - src/components/KetcherPanel.tsx
  - src/styles.css
  - src/components/HelpTour.tsx
  - src/components/HelpTour.module.css
  - src/App.tsx
  - src/components/__tests__/HelpTour.test.tsx
autonomous: false
requirements:
  - "Feature 2 — Guided Help Tour"
user_setup: []

must_haves:
  truths:
    - "A Help button appears next to Reset in the section-label-actions row"
    - "Clicking Help opens a stepped, spotlight-style overlay tour (not a centered modal)"
    - "The tour shows all 8 steps in order with a Back/Next/Close control + step counter"
    - "Each step spotlights a real UI region positioned from getBoundingClientRect()"
    - "The tour closes via Close button, Esc, backdrop click, or advancing past the last step"
    - "On an empty canvas, opening Help auto-loads Caffeine so every step has real content"
    - "The auto-loaded Caffeine stays on the canvas after the tour closes (no revert to empty)"
    - "If a molecule is already present, Help loads nothing and uses it as-is"
    - "The tour adds zero new runtime dependencies"
  artifacts:
    - path: "src/components/HelpTour.tsx"
      provides: "Custom stepped spotlight overlay: 8 steps, anchoring, controls, close paths"
      min_lines: 60
      exports: ["HelpTour"]
    - path: "src/components/HelpTour.module.css"
      provides: "Dimmer, spotlight cutout, callout card styling (oklch tokens)"
      contains: "oklch"
    - path: "src/components/KetcherPanel.tsx"
      provides: ".help-trigger button + onHelpClick prop"
      contains: "help-trigger"
    - path: "src/styles.css"
      provides: ".help-trigger styling mirroring .reset-trigger"
      contains: ".help-trigger"
    - path: "src/App.tsx"
      provides: "tourOpen state, handleHelpClick (empty-canvas Caffeine auto-load), HelpTour render"
      contains: "tourOpen"
  key_links:
    - from: "src/components/KetcherPanel.tsx"
      to: "App onHelpClick"
      via: "onClick prop on .help-trigger"
      pattern: "onHelpClick"
    - from: "src/App.tsx"
      to: "handleMolSelect('caffeine')"
      via: "empty-canvas auto-load in handleHelpClick"
      pattern: "handleMolSelect\\(.caffeine.\\)"
    - from: "src/App.tsx"
      to: "HelpTour"
      via: "render <HelpTour open={tourOpen} onClose=...>"
      pattern: "<HelpTour"
---

<objective>
Implement Feature 2 — Guided Help Tour. Add a Help button next to Reset that launches a custom, zero-dependency stepped spotlight overlay walking the user through 8 real UI regions, including the new pin feature. On an empty canvas, Help auto-loads Caffeine so every step has real content; the sample stays afterward.

Purpose: Demystify the tool's regions (editor, presets, InChI string, hover, pinning, InChIKey, legend, controls) for first-time chemists.
Output: `.help-trigger` button + prop, the new `HelpTour` overlay component + module CSS, App-owned tour state with empty-canvas Caffeine auto-load, and tour tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-pin-to-freeze-highlights-and-guided-help-tour/16-CONTEXT.md
@.planning/phases/16-pin-to-freeze-highlights-and-guided-help-tour/16-PATTERNS.md
@docs/superpowers/specs/2026-06-22-pin-freeze-and-help-tour-design.md

<interfaces>
<!-- Source-of-truth shapes the executor must build against. Extracted from codebase. -->

From src/components/KetcherPanel.tsx (current, lines 6-44) — the EXACT precedent to copy:
- Props interface has `onFeedbackClick?: () => void` and `onResetClick?: () => void` with JSDoc.
- The `section-label-actions` div renders `<button type="button" className="reset-trigger" onClick={onResetClick}>Reset</button>` and `feedback-trigger`. Add `.help-trigger` the same way, placed next to Reset.

From src/App.tsx (current):
- `const [selectedMolId, setSelectedMolId] = useState<string | null>(null)` — useState ownership idiom (lines 24-29).
- `handleMolSelect(id: string)` (lines 59-68) → `handleMolSelectLogic` — the preset-load path; sets `isSettingMoleculeRef` guard so debounced handleChange doesn't wipe selectedMolId. Reuse `handleMolSelect('caffeine')`, do NOT re-implement setMolecule.
- `handleReset` (lines 105-110) — App-owned async callback precedent.
- Empty-canvas detection: `useInchiStore.getState().layers.length === 0` (store read idiom used at App.tsx:75/118).
- KetcherPanel rendered at App.tsx:243-252 with `onResetClick={handleReset}`; FeedbackDialog rendered as a sibling at 238-242 — render `<HelpTour>` the same way.
- `ketcherRef` is `useRef<Ketcher | null>` (line 31).

From src/components/FeedbackDialog.tsx — structural template for HelpTour: typed props from App, no Ketcher ref, no store reads, local UI state via useState, `mountedRef` unmount guard for any async before setState.

From src/components/FeedbackDialog.module.css:29-32 — scrim literal `oklch(0.2 0.015 255 / 0.32)` (reuse for the dimmer). Surface tokens: `var(--bg-canvas)`, `1px solid var(--line)`, `border-radius: 8px`, `var(--font-serif)` title / `var(--font-sans)` body.

From src/styles.css:155-177 — `.reset-trigger` block is the verbatim template for `.help-trigger` (subdued secondary pill). Hover idiom: `oklch(from var(--line) calc(l - 0.04) c h)`.

Anchor regions already in the DOM: `canvas-wrap` class (KetcherPanel editor), `mol-list` (presets), InchiSection, InchiKeySection, the Legend, and the Reset/Help buttons.

The 8 tour steps (spec lines 94-104), titles in order:
1. The molecule editor  2. Presets list  3. The InChI string  4. Hovering  5. Pinning (click to freeze)  6. The InChIKey  7. The legend  8. Reset / Help buttons
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Help button (.help-trigger) to KetcherPanel + styling, and data-tour-id anchors</name>
  <files>src/components/KetcherPanel.tsx, src/styles.css</files>
  <read_first>
    - src/components/KetcherPanel.tsx (the file being modified — props interface 6-17, destructure 19-28, section-label-actions 33-44, canvas-wrap 48)
    - src/styles.css (the file being modified — .reset-trigger block ~155-177, .feedback-trigger block ~130-152, oklch hover idiom)
    - .planning/phases/16-pin-to-freeze-highlights-and-guided-help-tour/16-PATTERNS.md §"KetcherPanel.tsx" + §"styles.css" help-trigger section (lines 233-266, 356-375)
    - docs/superpowers/specs/2026-06-22-pin-freeze-and-help-tour-design.md §Trigger + §Anchoring (spec lines 74-93)
  </read_first>
  <action>In KetcherPanel: add `onHelpClick?: () => void` to the props interface (with JSDoc "Opens the guided Help tour. Rendered next to Reset on the section-label row."), add `onHelpClick` to the destructured params, and render a `<button type="button" className="help-trigger" onClick={onHelpClick}>Help</button>` inside `section-label-actions`, placed next to the existing Reset button (gated by `onHelpClick &&`). Add a `data-tour-id="editor"` attribute to the canvas-wrap container so the tour can anchor to the editor region via getBoundingClientRect(). In styles.css: add a `.help-trigger` rule copying the `.reset-trigger` block verbatim (28px height, pill `border-radius: 999px`, `var(--line)` bg, `var(--ink-soft)` color, `var(--font-sans)`, `cursor: pointer`), with `:hover` using the `oklch(from var(--line) calc(l - 0.04) c h)` idiom and a `:focus-visible` outline — oklch tokens only, no off-token colours (CLAUDE.md fidelity). Help-trigger styling lives in GLOBAL styles.css (these are global classes, not a CSS module).</action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "help-trigger" src/styles.css</automated>
  </verify>
  <acceptance_criteria>
    - src/components/KetcherPanel.tsx contains `onHelpClick` in the props interface AND a `className="help-trigger"` button in section-label-actions next to Reset.
    - src/components/KetcherPanel.tsx canvas-wrap container has `data-tour-id="editor"`.
    - src/styles.css contains a `.help-trigger` rule using `var(--...)` tokens and the `oklch(from var(--line) ...)` hover idiom (grep finds `.help-trigger`).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Help button renders next to Reset, styled to match Reset; editor region carries a tour anchor.</done>
</task>

<task type="auto">
  <name>Task 2: Build the HelpTour overlay component + module CSS (8 steps, anchoring, controls, close paths)</name>
  <files>src/components/HelpTour.tsx, src/components/HelpTour.module.css</files>
  <read_first>
    - src/components/FeedbackDialog.tsx (structural overlay template — typed props from App, local useState, mountedRef unmount guard 31-37)
    - src/components/FeedbackDialog.module.css (token + scrim idiom — scrim oklch literal ~29-32, surface tokens 8-16)
    - src/App.tsx (listener add/remove discipline reference — subscribe/unsubscribe + cleanup at 227-232)
    - .planning/phases/16-pin-to-freeze-highlights-and-guided-help-tour/16-PATTERNS.md §"HelpTour.tsx" + §"HelpTour.module.css" (lines 270-315)
    - docs/superpowers/specs/2026-06-22-pin-freeze-and-help-tour-design.md §Mechanism + §Anchoring + §Steps + §Close (spec lines 78-118)
  </read_first>
  <action>Create `src/components/HelpTour.tsx` exporting `HelpTour` with props `{ open: boolean; onClose: () => void }` (no Ketcher ref, no store reads — purely presentational, App-owned open state). Define the 8 steps as a const array of `{ title, body, tourId }` using the exact titles from the interfaces block and `data-tour-id` selectors: editor, presets (`mol-list`), inchi-string, hovering (anchors to the InChI string region too), pinning, inchikey, legend, controls. Hold local state: `stepIndex` and the computed target `DOMRect`. When `open`, render a full-viewport dimmer using the FeedbackDialog scrim oklch literal, a spotlight cutout positioned from `document.querySelector('[data-tour-id=...]').getBoundingClientRect()` (technique at executor's discretion: clip-path / box-shadow halo / overlay-with-hole), and a callout card (title + body) that picks a side (above/below/beside) from available viewport space so it never covers its target or runs off-screen. Controls: Back (disabled on step 0), Next, Close, and a step counter rendering "N of 8". Advancing past the last step calls `onClose`. Recompute the target rect on step change, window `resize`, and `scroll` — register resize/scroll listeners and an Esc keydown listener ONLY while open, removed in the useEffect cleanup (add-only-while-active discipline, spec risk line 146). Close paths: Close button, Esc, backdrop (dimmer) click, advancing past the last step. Use the `mountedRef` guard pattern if any async runs before setState. Create `src/components/HelpTour.module.css`: dimmer uses the scrim literal; callout card mirrors the dialog surface tokens (`var(--bg-canvas)`, `1px solid var(--line)`, `border-radius: 8px`, `var(--font-serif)` title / `var(--font-sans)` body); spotlight ring reuses a layer/accent token (e.g. `var(--c-formula)`) — oklch palette only, no off-token colours.</action>
  <verify>
    <automated>npx tsc --noEmit && grep -c "oklch" src/components/HelpTour.module.css</automated>
  </verify>
  <acceptance_criteria>
    - src/components/HelpTour.tsx exports `HelpTour`, takes `{ open, onClose }`, reads no store, and defines exactly 8 steps with the spec titles.
    - The component computes target position via `getBoundingClientRect()` and renders a step counter, Back, Next, Close.
    - resize/scroll/Esc listeners are registered inside a useEffect gated on `open` and removed in its cleanup (grep/source confirms cleanup returns removeEventListener).
    - Close fires on Close button, Esc, backdrop click, and advancing past step 8.
    - src/components/HelpTour.module.css uses `oklch(...)`/`var(--...)` tokens only (grep finds oklch; no raw hex/rgb colours introduced).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>HelpTour renders a stepped spotlight overlay over real regions with working navigation and all four close paths; styling on-token.</done>
</task>

<task type="auto">
  <name>Task 3: Wire tour state into App with empty-canvas Caffeine auto-load + tests</name>
  <files>src/App.tsx, src/components/__tests__/HelpTour.test.tsx</files>
  <read_first>
    - src/App.tsx (the file being modified — useState block 24-29, handleMolSelect 59-68, handleReset 105-110, empty-canvas read idiom 75/118, KetcherPanel render 243-252, FeedbackDialog sibling render 238-242)
    - src/components/HelpTour.tsx (the contract from Task 2 — props { open, onClose })
    - src/components/KetcherPanel.tsx (now exposes onHelpClick from Task 1)
    - src/data/molecules.ts (caffeine preset id `'caffeine'`, line 24 — the D-02 auto-load target)
    - src/components/__tests__/FeedbackDialog.test.tsx (overlay component test analog)
    - .planning/phases/16-pin-to-freeze-highlights-and-guided-help-tour/16-PATTERNS.md §"App.tsx" + §"Test mirroring" (lines 318-352, 432-440)
    - docs/superpowers/specs/2026-06-22-pin-freeze-and-help-tour-design.md §Empty-canvas handling + §Testing (spec lines 106-109, 141-142)
  </read_first>
  <action>In App: add `const [tourOpen, setTourOpen] = useState(false)`. Add an async `handleHelpClick`: detect empty canvas via `useInchiStore.getState().layers.length === 0`; if empty and `ketcherRef.current`, `await handleMolSelect('caffeine')` to auto-load Caffeine (reuses the existing preset path + its isSettingMoleculeRef guard — do NOT re-implement setMolecule, do NOT call setMolecule('')); then `setTourOpen(true)`. If a molecule is already present, load nothing. Pass `onHelpClick={handleHelpClick}` to `KetcherPanel`. Render `<HelpTour open={tourOpen} onClose={() => setTourOpen(false)} />` as a sibling alongside FeedbackDialog. CRITICAL: onClose only sets tourOpen false — never call setMolecule('') or resetAll() on close, so the auto-loaded Caffeine STAYS (D-02, spec line 108). Add `data-tour-id` anchors to the remaining tour regions if not already present (presets `mol-list`, InchiSection, InchiKeySection, legend, controls) so the overlay can find them. Write `src/components/__tests__/HelpTour.test.tsx`: renders all 8 steps in order; Back/Next/counter ("N of 8") behave; Close button, Esc, backdrop click, and advancing past the last step all close (onClose called); empty-canvas opening triggers a preset load and the sample remains after close; populated canvas loads nothing. Where a test needs InChI data, use a REAL InChI fixture — never fabricated layer text (memory feedback_real_domain_fixtures_and_gates). FIXTURE SOURCE (no in-repo test currently uses Caffeine; existing real fixtures are benzene/ethanol/alanine/toluene): for the empty-canvas-auto-loads-Caffeine assertion, prefer asserting that `handleMolSelect('caffeine')` was invoked / a preset load fired and the canvas is non-empty after — rather than grepping for a specific Caffeine InChI string. If you DO need the verbatim Caffeine InChI, derive it by loading the SMILES at molecules.ts:24 (`CN1C=NC2=C1C(=O)N(C(=O)N2C)C`) through Ketcher's `getInchi(true)` and copy the exact emitted string (do NOT hand-write it). For tour-navigation/state tests where the specific molecule is immaterial, reuse an existing real fixture (e.g. alanine `InChI=1S/C3H7NO2/...`). This avoids a fabricated-string regression.</action>
  <verify>
    <automated>npx vitest run src/components/__tests__/HelpTour.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `tourOpen` state, a `handleHelpClick` that auto-loads `handleMolSelect('caffeine')` only when `layers.length === 0`, and renders `<HelpTour open={tourOpen} onClose={...}>`.
    - The onClose handler does NOT call setMolecule('') or resetAll() (grep/source confirms — sample stays).
    - HelpTour.test.tsx asserts all 8 steps render in order, all four close paths fire onClose, empty-canvas loads a preset, populated canvas loads nothing.
    - HelpTour.test.tsx asserts the empty-canvas auto-load via the preset path (e.g. `handleMolSelect('caffeine')` invoked / canvas non-empty after open), and any InChI data used is a REAL fixture (grep finds an `InChI=1S/` literal — verbatim Caffeine `InChI=1S/C8H10N4O2/...` derived via Ketcher from molecules.ts:24, or a reused real fixture such as alanine `InChI=1S/C3H7NO2/...`), never fabricated layer text.
    - `npx vitest run src/components/__tests__/HelpTour.test.tsx` and `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Help opens the tour; empty canvas auto-loads Caffeine that persists after close; populated canvas untouched; tour tests green on real fixtures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Human-verify the Help tour + pin interaction</name>
  <what-built>The Help button + custom stepped spotlight tour (8 steps), with empty-canvas Caffeine auto-load and click-to-pin from plan 16-01 (a separate wave-1 plan; the pin step below assumes 16-01 is already merged). This checkpoint verifies the visual/interaction quality that automated tests cannot fully cover (spotlight positioning, callout side-selection, on-token styling, listener lifecycle not eating clicks).</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in a browser.
    2. With an empty canvas, click **Help** (next to Reset). Expect: Caffeine auto-loads and the tour opens on step 1 (the editor) with a dimmed page and a spotlight on the editor region.
    3. Click **Next** through all 8 steps. Expect: each step spotlights the correct real region (editor → presets → InChI string → hovering → pinning → InChIKey → legend → Reset/Help buttons), the callout never covers its target or runs off-screen, and the counter reads "N of 8". **Back** returns to the prior step.
    4. Resize the window mid-tour. Expect: the spotlight + callout reposition correctly.
    5. Close the tour by advancing past step 8; then reopen and close via the **Close** button, **Esc**, and a **backdrop click**. Expect: all four paths close it, and Caffeine REMAINS on the canvas after closing.
    6. (Pin interaction — delivered by plan **16-01**; run this step only AFTER both wave-1 plans, 16-01 and 16-02, are merged. If 16-01 is not yet merged, skip step 6, approve the tour-only steps 1-5/7, and re-run step 6 once 16-01 lands.) With Caffeine still loaded, hover an InChI layer (transient highlight), then **click** a layer chunk. Expect: the highlight + explanation freeze, the chunk shows a persistent ring (locked cue), and the "Pinned — click anywhere or press Esc to release." hint appears. Hover elsewhere → nothing changes. Click anywhere → releases. Click a sub-token (atom/bond) → that precise sub-token freezes. Press **Esc** while pinned → releases. After releasing, confirm normal clicks elsewhere on the page (buttons, presets) still work (listener was removed).
    7. (Also part of 16-01's pin feature — run together with step 6 after both wave-1 plans merge.) Confirm the displayed InChI string text is byte-for-byte unchanged by pinning/clicking.
  </how-to-verify>
  <action>Pause and run the manual verification in <how-to-verify> against `npm run dev`. Do not auto-advance — this is a blocking human-verify gate covering spotlight positioning, callout side-selection, on-token styling, listener lifecycle (no eaten clicks), Caffeine persistence after close, and the full pin/unpin/Esc interaction including string passthrough.</action>
  <verify><human-check>Operator confirms all steps in <how-to-verify> pass, then types "approved".</human-check></verify>
  <resume-signal>Type "approved" or describe issues (e.g. callout overlaps target on step 3, ring colour wrong, clicks eaten after unpin).</resume-signal>
  <done>Operator has visually verified the Help tour (8 steps, all close paths, Caffeine persistence) and the pin interaction (freeze, release, Esc, sub-token, no eaten clicks, verbatim string) and typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click/keyboard → DOM | Only trust boundary. No network, no backend, no untrusted/serialized input. Tour copy is static; the auto-loaded molecule is a hardcoded preset SMILES from molecules.ts, not user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Denial of Service (self-inflicted UI lock) | HelpTour resize/scroll/Esc + backdrop listeners | mitigate | Listeners registered ONLY while the tour is open and removed in the useEffect cleanup on close (spec risk line 146). Prevents leftover listeners eating clicks after close. Covered by Task 2 acceptance + the human-verify checkpoint step 5/6. |
| T-16-05 | Information Disclosure / Injection (XSS) | tour callout copy + step titles | accept | All step titles/bodies are static string literals in HelpTour.tsx; no `dangerouslySetInnerHTML`, no user-controlled string rendered. No XSS sink. |
| T-16-06 | Tampering (string integrity) | empty-canvas auto-load path | mitigate | Auto-load reuses `handleMolSelect('caffeine')` (existing tested preset path with the isSettingMoleculeRef guard); the displayed InChI remains Ketcher's verbatim output. No re-join of parsed fields. Verified in checkpoint step 7. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | ZERO new runtime dependencies (D-01 / CLAUDE.md lean-deps; custom React + CSS tour). No install task exists; no Package Legitimacy Gate needed. |
</threat_model>

<verification>
- `npx vitest run` (full suite) exits 0 — HelpTour tests green, no regressions in existing 343 tests.
- `npx tsc --noEmit` exits 0.
- Human-verify checkpoint approved (spotlight positioning, callout side-selection, listener lifecycle, on-token styling, Caffeine persistence, pin interaction).
- Grep gate: no new runtime deps — `package.json` dependencies unchanged (only ketcher-*, react, react-dom, zustand).
</verification>

<success_criteria>
- Help button renders next to Reset, styled on-token.
- HelpTour is a custom zero-dependency stepped spotlight overlay with 8 steps, Back/Next/Close + counter, anchoring via getBoundingClientRect(), and all four close paths.
- Empty canvas auto-loads Caffeine which persists after close; populated canvas loads nothing.
- Listeners added only while the tour is open and removed on close.
- All tests use real InChI fixtures and pass; tsc clean; human-verify approved.
</success_criteria>

<output>
Create `.planning/phases/16-pin-to-freeze-highlights-and-guided-help-tour/16-02-SUMMARY.md` when done.
</output>
