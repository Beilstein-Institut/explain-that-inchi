---
context: phase
phase: 16-pin-to-freeze-highlights-and-guided-help-tour
task: 4
total_tasks: 7
status: in_progress
last_updated: 2026-06-22T13:33:06.121Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure this session.

- [ ] CONSTRAINT: **`tsc --noEmit` is NOT equivalent to the production build** — The executor verified 16-02 with `npx tsc --noEmit` (passed), but `npm run build` (`tsc -b`, project references + `noUnusedLocals`) FAILED on an unused import. Structural mitigation: the post-merge gate MUST run `npm run build`, not just `tsc --noEmit`, before declaring a wave green. (Already fixed this session — commit ec242ab.)

**Do not proceed until the box is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| tsc --noEmit ≠ build | Executor's `tsc --noEmit` passed but `tsc -b` (production build) failed on an unused `beforeEach` import in HelpTour.test.tsx | advisory | Post-merge gate runs `npm run build` (the real build), not just `tsc --noEmit` |
| Undeclared cross-plan file edits | 16-02 edited InchiSection.tsx / InchiKeySection.tsx / Legend.tsx to add `data-tour-id` anchors — none were in its `files_modified` frontmatter, so the intra-wave overlap check could not flag the InchiSection.tsx overlap with 16-01 | advisory | Merges happened to be conflict-free (disjoint lines). Future planning: list ALL touched files in `files_modified`, including anchor-only edits |

<current_state>
Phase 16 BUILD IS COMPLETE and merged to master. Both wave-1 plans (16-01 click-to-pin, 16-02 Help tour) are done, merged (commits 37bfaf5, d12fce1), and the post-merge gate passed after one fix (ec242ab). Working tree is CLEAN at master @ ec242ab.

Automated status: `npm run build` exits 0, full suite **375/375 passed (20 files)**, `tsc` clean.

We are paused at the single remaining gate: **16-02 Task 4 — the blocking human-verify checkpoint** (visual/interaction test of the tour + pin feature). The dev server was started, the checkpoint was presented to the user, then the user asked to stop the server and hand off. The server is now STOPPED.
</current_state>

<completed_work>

Completed Tasks (all committed on master):
- 16-01 Task 1: pinned state machine + hover gate (store.ts) — 1b8313b
- 16-01 Task 2: layer + sub-token click-to-pin (InchiSection + LayerText) — 2ccf04c
- 16-01 Task 3: pinned precedence in hook + Explanation + locked-ring CSS — 6fcced2 (verified + committed by orchestrator after executor lost Bash mid-run)
- 16-01 SUMMARY.md — bb6336b
- 16-02 Task 1: Help button (.help-trigger) + styling + editor anchor — c5288a5
- 16-02 Task 2: HelpTour 8-step spotlight overlay + module CSS — b461008
- 16-02 Task 3: App wiring + empty-canvas Caffeine auto-load + tests — d1024a2
- 16-02 SUMMARY.md — 708f3b0
- Merge both worktrees + post-merge fix (unused import) — 37bfaf5, d12fce1, ec242ab
</completed_work>

<remaining_work>

1. **16-02 Task 4 — human-verify checkpoint (BLOCKING).** Restart dev server, have the user verify in a browser. Steps below in <next_action>.
2. On "approved": `gsd-sdk query roadmap.update-plan-progress 16 16-01 complete` and same for 16-02.
3. **Code review gate** (REQUIRED, advisory): `Skill(gsd-code-review, "16")`, then read `16-REVIEW.md` status.
4. **Regression gate**: run prior-phase tests (npm test) for cross-phase regressions.
5. **verify_phase_goal**: spawn `gsd-verifier` (model sonnet) → creates `16-VERIFICATION.md`. Phase goal from ROADMAP; req IDs: "Feature 1 — Click-to-Pin (Freeze) Highlights"; "Feature 2 — Guided Help Tour".
6. **Complete**: `gsd-sdk query phase.complete 16` → marks roadmap/STATE/REQUIREMENTS, then commit.
</remaining_work>

<decisions_made>

- Ran 16-01 and 16-02 in **parallel git worktrees** (no declared files_modified overlap; independent build work).
- **Deferred 16-02's human-verify checkpoint to the orchestrator post-merge** — its pin steps (6-7) test 16-01's feature, absent in 16-02's isolated worktree; the plan explicitly gates it to run after both plans merge.
- **Orchestrator finished 16-01 Task 3 inline** (ran the plan's gates, committed) because the executor's Bash was revoked mid-run after writing the code — no logic was changed.
- Branching strategy = **none** → all work on `master`.
- executor/verifier model = **sonnet**; parallelization = true; worktrees = true; human_verify_mode = end-of-phase (default).
</decisions_made>

<blockers>
- 16-02 Task 4 human-verify: needs a human in a browser. Not automatable. Restart server and present.
</blockers>

## Required Reading (in order)
1. `.planning/phases/16-pin-to-freeze-highlights-and-guided-help-tour/16-02-PLAN.md` — the Task 4 checkpoint `<how-to-verify>` is the exact verification script (8 tour steps + pin steps 6-7)
2. `.planning/HANDOFF.json` — machine-readable state mirror of this file
3. `16-01-SUMMARY.md` and `16-02-SUMMARY.md` — what was built, with the deviation notes

## Infrastructure State
- **Dev server: STOPPED** (was Vite at http://localhost:5173/explain-that-inchi/ — base path is `/explain-that-inchi/`, not root). Restart with `npm run dev`.
- Git: clean working tree, `master` @ ec242ab. Both worktrees removed; their temp branches deleted.
- Note: several unrelated orphan `worktree-agent-*` branches from PRIOR sessions remain (no worktrees attached). Left untouched on purpose.

<context>
The hard part is done and verified-green. Resuming is mechanical: restart the server, walk the user through the checkpoint, then run the four closing gates (code review → regression → verifier → complete). If the user reports issues during human-verify, that routes to gap closure (`/gsd:plan-phase 16 --gaps`) instead of completion. Do NOT re-run the executors — the build is committed and passing.
</context>

<next_action>
Start with: `npm run dev` (background), confirm http://localhost:5173/explain-that-inchi/ returns 200, then re-present the 16-02 Task 4 human-verify checkpoint. Verification script (from 16-02-PLAN.md):
1. Empty canvas → click Help → Caffeine auto-loads, tour opens on step 1 (editor), page dimmed + spotlight.
2. Next through all 8 steps (editor→presets→InChI string→hovering→pinning→InChIKey→legend→Reset/Help); callout never covers target/off-screen; counter "N of 8"; Back works.
3. Resize mid-tour → spotlight + callout reposition.
4. Close via: advance past step 8, Close button, Esc, backdrop click — Caffeine REMAINS after each.
5. Pin: hover layer (transient) → click chunk → freezes + ring + hint; hover elsewhere = no change; click anywhere = release; click sub-token = precise freeze; Esc = release; after release, normal clicks still work.
6. InChI string text byte-for-byte unchanged by pinning.
On "approved" → roadmap.update-plan-progress (16-01, 16-02) → code review → regression → verifier → phase.complete.
</next_action>
