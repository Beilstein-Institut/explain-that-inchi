---
context: phase
phase: 16-pin-to-freeze-highlights-and-guided-help-tour
task: 9
total_tasks: 13
status: in_progress
last_updated: 2026-06-24T16:41:15.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. Prior constraint (tsc --noEmit ≠ production build) was resolved in ec242ab._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| tsc --noEmit ≠ build | Executor's `tsc --noEmit` passed but `tsc -b` (production build) failed on an unused `beforeEach` import in HelpTour.test.tsx | advisory | Post-merge gate runs `npm run build` (the real build), not just `tsc --noEmit` |

<current_state>
UAT in progress for Phase 16. Tests 1–8 passed. Currently awaiting
user response on Test 9 (navigate through all 8 tour steps).
All build work complete and merged. Code review fixes applied (WR-01..WR-04, 961776f).
375/375 tests green. 16-UAT.md is modified (uncommitted).
</current_state>

<completed_work>
- Human-verify checkpoint: approved 2026-06-23
- Regression gate: 375/375 green (post code-review-fix)
- Code review (gsd-code-review 16): 4 warnings, 3 info — 48a42a2
- Code review fixes WR-01..WR-04 applied: store.ts (setInchiData resets pinned), HelpTour.tsx
  (mountedRef in useEffect, useLayoutEffect for step reset), store.test.ts (beforeEach completeness) — 961776f
- UAT Test 1: click-to-pin freezes highlight — PASS
- UAT Test 2: pinned highlight doesn't change on hover elsewhere — PASS
- UAT Test 3: Esc key releases the pin — PASS
- UAT Test 4: click pinned chunk again to release — PASS
- UAT Test 5: click outside releases the pin — PASS
- UAT Test 6: sub-token click-to-pin — PASS
- UAT Test 7: Help button visible in toolbar — PASS
- UAT Test 8: click Help opens 8-step tour — PASS
</completed_work>

<remaining_work>
- UAT Test 9 (IN PROGRESS): navigate through all 8 tour steps
- UAT Test 10: all 4 close paths dismiss the tour
- UAT Test 11: empty canvas auto-loads Caffeine on Help click
- UAT Test 12: non-empty canvas — tour opens without replacing the molecule
- UAT Test 13: reopening the tour starts from step 1
- verify_phase_goal via gsd-verifier → VERIFICATION.md
- phase.complete: mark phase 16 done in ROADMAP.md / STATE.md / REQUIREMENTS
</remaining_work>

<decisions_made>
- WR-01: setInchiData now resets pinned to null (stale pin survived preset load)
- WR-02: mountedRef in HelpTour moved to useEffect with cleanup (was non-functional at render time)
- WR-03: step reset uses useLayoutEffect (prevents one-frame flash on tour re-open)
- WR-04: store.test.ts beforeEach resets all Phase 16 fields (prevents Zustand singleton test leaks)
</decisions_made>

<blockers>
None — UAT is the only remaining gate before phase close.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — full structured state with all completed/remaining task IDs
2. `.planning/phases/16-pin-to-freeze-highlights-and-guided-help-tour/16-UAT.md` — UAT progress (8/13 passed, test 9 in progress)

## Infrastructure State
- Dev server: STOPPED. Restart with `npm run dev`, app at http://localhost:5173/explain-that-inchi/
- Git: master @ c1deaa8, 16-UAT.md modified (uncommitted), package-lock.json modified

<context>
Phase 16 is essentially done. Just need to walk through the remaining UAT checklist (tests 9–13),
then run the verifier and close the phase. All interactive tests require a browser on localhost:5173.
Use /gsd-verify-work 16 to resume the conversational UAT from the existing 16-UAT.md file.
</context>

<next_action>
Resume UAT: run /gsd-resume-work → picks up HANDOFF.json → continues UAT at Test 9.
Or run /gsd-verify-work 16 directly to resume the existing 16-UAT.md session.
Test 9 prompt: "Open the tour, click Next through all 8 steps — confirm spotlight and callout move to correct targets, counter reads 1 of 8 … 8 of 8, and Next becomes Finish on the last step."
</next_action>
