---
phase: "10"
plan: "04"
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/App.tsx
  - src/components/__tests__/FeedbackDialog.test.tsx
autonomous: true
requirements:
  - FEED-03
gap_closure: true

must_haves:
  truths:
    - "When the feedback dialog is opened with a molecule drawn or loaded, the context-preview panel shows the molecule's SMILES string instead of '(none)'"
    - "When the feedback dialog is opened with an empty canvas, the context-preview panel still shows '(none)' gracefully"
    - "The SMILES displayed in the preview matches the molecule that was drawn or loaded at the moment the dialog was opened"
    - "Submit-time SMILES collection in handleFeedbackSubmit is unchanged — it remains the source of truth for what is actually attached to the issue"
  artifacts:
    - path: "src/App.tsx"
      provides: "previewSmiles state + async SMILES fetch on dialog open"
      contains: "previewSmiles"
    - path: "src/components/__tests__/FeedbackDialog.test.tsx"
      provides: "Regression test: preview renders a SMILES that is explicitly supplied"
      min_lines: 270
  key_links:
    - from: "App.tsx onFeedbackClick handler"
      to: "ketcherRef.current.getSmiles()"
      via: "async call with silent-catch before showModal()"
      pattern: "getSmiles.*previewSmiles|previewSmiles.*getSmiles"
    - from: "App.tsx contextPreview"
      to: "FeedbackDialog contextPreview prop"
      via: "smiles: previewSmiles"
      pattern: "smiles: previewSmiles"
---

<objective>
Close the UAT gap from Test 3: the feedback context-preview panel displays "SMILES: (none)" even when a molecule is drawn or loaded. The fix populates `contextPreview.smiles` by calling `getSmiles()` once per dialog-open event and storing the result in React state, then passing it into the existing `contextPreview` object. No structural changes — submit path, FeedbackDialog component, and KetcherPanel mount are all preserved.

Purpose: The preview claims to show "what gets attached" — showing "(none)" for SMILES when the molecule IS there is actively misleading to the user. The SMILES is already collected at submit time (D-12); the only gap is the preview.
Output: Updated `src/App.tsx` (previewSmiles state + on-open fetch) and one new test in `FeedbackDialog.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-feedback-dialog-context-capture-entry-point/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate previewSmiles state on dialog open in App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Add a `previewSmiles` React state variable (`useState&lt;string | undefined&gt;(undefined)`) alongside the other state declarations near the top of `App`.

    Replace the inline `onFeedbackClick` arrow prop on `&lt;KetcherPanel&gt;` with a named async handler `handleFeedbackOpen`. This handler must:
    1. Call `ketcherRef.current?.getSmiles()` wrapped in a try/catch; on success set `previewSmiles` to the result, on catch set it to `undefined` (silent fallback per D-12).
    2. Call `dialogRef.current?.showModal()` after the SMILES attempt — whether it succeeded or failed.

    Update `contextPreview.smiles` to use `previewSmiles` instead of the hardcoded `undefined`. The rest of `contextPreview` (inchi, presetName, userAgent, appVersion) is unchanged.

    The `handleFeedbackSubmit` function must NOT be changed — it already calls `getSmiles()` independently at submit time and remains the source of truth for the attached context.

    Do NOT add `previewSmiles` to the Zustand store (feedback is ephemeral UI state per the carry-forward decisions).
    Do NOT move, reorder, or conditionally render KetcherPanel or FeedbackDialog — HMR remount hazard.
    Do NOT pass `handleFeedbackOpen` as anything other than the `onFeedbackClick` prop — the KetcherPanel interface already expects `() =&gt; void` or `() =&gt; Promise&lt;void&gt;`; an async function is assignable here without type changes.

    After the change the comment above `contextPreview` should read: "smiles is fetched once per dialog-open (handleFeedbackOpen); undefined until the user opens the dialog or if getSmiles() throws (D-12)." Remove the old comment that says "live getSmiles() is only called at submit time".
  </action>
  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npx tsc -b --noEmit && npx vitest run --reporter=verbose 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    - `src/App.tsx` contains `previewSmiles` state
    - `contextPreview.smiles` is `previewSmiles` (not `undefined` literal)
    - An async `handleFeedbackOpen` (or inline async arrow) calls `getSmiles()` then `showModal()`
    - `handleFeedbackSubmit` body is unchanged
    - `tsc -b --noEmit` exits 0
    - `vitest run` passes all existing tests (no regressions)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression test — preview renders supplied SMILES string</name>
  <files>src/components/__tests__/FeedbackDialog.test.tsx</files>
  <behavior>
    - Test: when `contextPreview.smiles` is a non-empty string (e.g. `'CC(=O)O'`), the context-preview `&lt;pre&gt;` block contains that string.
    - Test: when `contextPreview.smiles` is `undefined`, the `&lt;pre&gt;` block contains the D-15 placeholder `'(none)'`.
    (The second behavior is already covered by the existing "Phase 9 placeholders" test; add or tighten a test that specifically names the SMILES field so the gap cannot silently regress.)
  </behavior>
  <action>
    Add one new `it` block to the existing `describe('FeedbackDialog')` suite in `FeedbackDialog.test.tsx`:

    Name: `'context preview renders supplied SMILES string (gap-10-UAT-T3)'`

    The test renders `&lt;FeedbackDialog&gt;` with `contextPreview={{ smiles: 'CC(=O)O' }}` (other fields omitted / undefined) and asserts that `document.querySelector('pre')?.textContent` contains `'CC(=O)O'` and does NOT contain `'(none)'` for the SMILES line.

    Do NOT modify any existing tests. Do NOT change the import list beyond what is needed (all required helpers are already imported).

    The test must pass with the current `FeedbackDialog.tsx` implementation — no component changes are needed because `FeedbackDialog` already renders whatever `contextPreview.smiles` provides; the gap was purely in `App.tsx` not supplying the value.
  </action>
  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npx vitest run src/components/__tests__/FeedbackDialog.test.tsx --reporter=verbose 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - New test `'context preview renders supplied SMILES string (gap-10-UAT-T3)'` exists in the suite
    - All 10 tests in FeedbackDialog.test.tsx pass (9 existing + 1 new)
    - `vitest run` exits 0 across the full suite
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ketcher WASM → App.tsx | `getSmiles()` return value is untrusted molecule data from the WASM process; displayed verbatim in a read-only `&lt;pre&gt;` element (no eval, no innerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04-01 | Information Disclosure | previewSmiles state | accept | SMILES is molecule data the user drew themselves; displayed in a `&lt;pre&gt;` block that already showed InChI — no new disclosure surface |
| T-10-04-02 | Denial of Service | getSmiles() in handleFeedbackOpen | mitigate | silent catch — if `getSmiles()` throws or hangs, `previewSmiles` stays `undefined` and `showModal()` still fires; dialog open is never blocked |
| T-10-04-SC | Tampering | npm/pip/cargo installs | accept | no new packages added in this plan |
</threat_model>

<verification>
1. `tsc -b --noEmit` exits 0 — no type errors introduced.
2. `vitest run` exits 0 — all tests pass (no regressions, new test passes).
3. Browser verify (human): open the app, load any preset molecule (e.g. Alanine), click "Send feedback" — the context-preview `&lt;pre&gt;` block must show the molecule's SMILES string, not "(none)".
4. Browser verify (human): open the app with an empty canvas, click "Send feedback" — the preview must show "(none)" for SMILES.
5. Browser verify (human): submit the dialog after step 3 — the GitHub issue body must still contain the correct SMILES (submit path unchanged).
</verification>

<success_criteria>
- With a preset or drawn molecule, opening the feedback dialog shows the molecule's SMILES in the preview panel.
- With an empty canvas, the preview shows "(none)" for SMILES — no error, no crash.
- `tsc -b --noEmit` exits 0.
- `vitest run` passes all tests (10 in FeedbackDialog suite, no regressions elsewhere).
- `handleFeedbackSubmit` in App.tsx is byte-for-byte unchanged.
- FeedbackDialog.tsx is not modified.
- KetcherPanel position in JSX is not changed.
</success_criteria>

<output>
Create `.planning/phases/10-feedback-dialog-context-capture-entry-point/10-04-SUMMARY.md` when done.
</output>
