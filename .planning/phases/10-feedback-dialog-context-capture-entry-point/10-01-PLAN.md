---
phase: "10"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/buildFeedbackUrl.ts
  - src/lib/__tests__/buildFeedbackUrl.test.ts
autonomous: true
requirements:
  - FEED-08

must_haves:
  truths:
    - "buildFeedbackUrl returns a fullBody field containing the full untruncated issue body"
    - "fullBody is present on both truncated and non-truncated results"
    - "fullBody matches the body before any truncation was applied"
    - "All existing 244+ tests continue to pass"
  artifacts:
    - path: "src/lib/buildFeedbackUrl.ts"
      provides: "BuildFeedbackUrlResult interface with fullBody field + return statement updated"
      contains: "fullBody: initialBody"
    - path: "src/lib/__tests__/buildFeedbackUrl.test.ts"
      provides: "D-11 test coverage — fullBody present, fullBody contains untruncated InChI on truncation"
      contains: "D-11: fullBody field"
  key_links:
    - from: "src/lib/buildFeedbackUrl.ts"
      to: "src/components/FeedbackDialog.tsx (Plan 02)"
      via: "BuildFeedbackUrlResult.fullBody"
      pattern: "fullBody.*initialBody"
---

<objective>
Extend the Phase 9 pure builder to expose the full untruncated issue body alongside the URL.

Purpose: The dialog's clipboard fallback path (D-10/D-11) must copy the full untruncated issue body. Per D-11 and the project rule against reconstructing strings, the dialog MUST NOT re-assemble the Phase 9 body template. The single source of truth is the builder module itself. Adding `fullBody` to `BuildFeedbackUrlResult` gives the dialog a passthrough without any template duplication.

Output: `BuildFeedbackUrlResult` gains a `fullBody: string` field; the builder's return statement includes `fullBody: initialBody` (the body string before truncation). Existing tests stay green; two new tests verify the D-11 contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-feedback-dialog-context-capture-entry-point/10-CONTEXT.md
@.planning/phases/09-feedback-url-builder-config-version-injection/09-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fullBody to BuildFeedbackUrlResult and return it</name>
  <files>src/lib/buildFeedbackUrl.ts</files>
  <read_first>
    src/lib/buildFeedbackUrl.ts — read the entire file; key points: BuildFeedbackUrlResult interface (lines 41–47), the initialBody assignment (line 205), and the return statement (line 258) which currently returns `{ url, truncated }`.
  </read_first>
  <behavior>
    - BuildFeedbackUrlResult interface gains a third field: `fullBody: string` with JSDoc "Full untruncated issue body (message + full context). Single source of truth for the clipboard fallback (D-11) — never re-assembled by the dialog."
    - The `buildFeedbackUrl` function return statement (currently `return { url, truncated }`) becomes `return { url, truncated, fullBody: initialBody }` where `initialBody` is the variable already assigned on line 205.
    - No other logic changes. The truncation loop is untouched. The internal `buildBody()` helper is untouched.
    - TypeScript must compile cleanly: `tsc -b` exits 0.
  </behavior>
  <action>
    In `BuildFeedbackUrlResult`, add field `fullBody: string` with JSDoc per D-11: "Full untruncated issue body (message + full context). Single source of truth for the clipboard fallback (D-11) — never re-assembled by the dialog."

    In the `buildFeedbackUrl` function body, the variable `initialBody` is already assigned before the truncation loop (line 205: `const initialBody = buildBody(category, message, context)`). The existing return statement `return { url, truncated }` at the bottom of the function must become `return { url, truncated, fullBody: initialBody }`.

    No other changes to the file.
  </action>
  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npx tsc -b --noEmit && echo "tsc clean"</automated>
  </verify>
  <done>BuildFeedbackUrlResult has a fullBody field; return statement includes fullBody: initialBody; tsc exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add D-11 test coverage for the fullBody field</name>
  <files>src/lib/__tests__/buildFeedbackUrl.test.ts</files>
  <read_first>
    src/lib/__tests__/buildFeedbackUrl.test.ts — read entire file to understand the existing describe/it/expect pattern and import line; locate the last describe block to append after it without disturbing existing tests.
  </read_first>
  <behavior>
    Two new test cases inside a new `describe('D-11: fullBody field', ...)` block appended to the existing test file:

    Test 1 — "result includes fullBody for non-truncated input":
      - Call buildFeedbackUrl with category 'Bug', message 'Test', context `{ inchi: 'InChI=1S/C6H6/c1-2-4-6-5-3-1/h1-6H', smiles: 'c1ccccc1' }`
      - Expect `fullBody` to be defined (not undefined, not empty)
      - Expect `fullBody` to contain 'Test' (the user message is present)
      - Expect `fullBody` to contain 'InChI=1S/C6H6' (the InChI is present)

    Test 2 — "fullBody contains the untruncated InChI even when url is truncated":
      - Construct LONG_INCHI by repeating a real InChI fragment 100 times
      - Call buildFeedbackUrl with that LONG_INCHI and a long SMILES
      - Expect `truncated` to be true (budget exceeded)
      - Expect `fullBody` to contain the FULL LONG_INCHI (not the truncated version from the url)
  </behavior>
  <action>
    Append a new `describe('D-11: fullBody field', () => { ... })` block at the end of the existing test file (after the last closing brace of the existing describes, before the final empty line if any). Import `buildFeedbackUrl` is already present.

    Test 1: name "result includes fullBody for non-truncated input". Destructure `{ fullBody }` from `buildFeedbackUrl({ message: 'Test', category: 'Bug', context: { inchi: 'InChI=1S/C6H6/c1-2-4-6-5-3-1/h1-6H', smiles: 'c1ccccc1' } })`. Assert `fullBody` is truthy, contains 'Test', contains 'InChI=1S/C6H6'.

    Test 2: name "fullBody contains the untruncated InChI even when url is truncated". Build `const LONG_INCHI = 'InChI=1S/C7H8.C6H6/c1-7-5-3-2-4-6-7;1-2-4-6-5-3-1/h2-6H,1H3;1-6H'.repeat(100)`. Call `buildFeedbackUrl({ message: 'My feedback', category: 'Bug', context: { inchi: LONG_INCHI, smiles: 'Cc1ccccc1.'.repeat(200) } })`. Assert `truncated` is true. Assert `fullBody` contains LONG_INCHI (i.e., the full unrepeated string).

    No changes to existing test cases.
  </action>
  <verify>
    <automated>cd /home/bsmue/code/explain-that-inchi && npx vitest run src/lib/__tests__/buildFeedbackUrl.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>vitest run exits 0; both D-11 tests pass; total test count increases by 2; no previously passing test regresses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lib/buildFeedbackUrl.ts (pure module) | No browser globals — only internal string manipulation. No new trust surface introduced by adding the fullBody field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-SC | Tampering | npm/vitest install | accept | No new packages installed in this plan; existing vitest dependency already audited in Phase 9. |

No new trust boundary introduced. The `fullBody` field is an additional output from an already-pure function; the caller (dialog) will use it only for `navigator.clipboard.writeText` — that surface is addressed in Plan 02's threat model. The builder itself has no network, no DOM, no auth surface.
</threat_model>

<verification>
Run the full test suite to confirm no regressions:
`cd /home/bsmue/code/explain-that-inchi && npx vitest run 2>&1 | tail -5`

Expected: all existing 244+ tests pass plus 2 new D-11 tests. `tsc -b --noEmit` exits 0.

Grep check — confirm fullBody is in the return statement:
`grep -n 'fullBody' /home/bsmue/code/explain-that-inchi/src/lib/buildFeedbackUrl.ts`
Expected lines: the interface field and the return statement.
</verification>

<success_criteria>
- `BuildFeedbackUrlResult` interface in buildFeedbackUrl.ts contains field `fullBody: string`
- `buildFeedbackUrl` return statement includes `fullBody: initialBody`
- `vitest run` exits 0 with 2 new D-11 tests passing and no regressions
- `tsc -b --noEmit` exits 0
</success_criteria>

<output>
Create `.planning/phases/10-feedback-dialog-context-capture-entry-point/10-01-SUMMARY.md` when done.
</output>
