---
context: default
phase: none
task: null
total_tasks: null
status: paused
last_updated: 2026-09-03T13:10:39Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Carried forward from the 2026-08-26 handoff. Each was discovered through actual failure in this
> repo, not predicted. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **Never `git checkout <file>` while work is uncommitted** — it once wiped a whole
      pass's edits to `KetcherPanel.tsx`.
- [ ] CONSTRAINT: **Verify every commit in isolation when splitting a tree** — hunk staging once merged
      adjacent hunks from different changes into one broken commit.
- [ ] CONSTRAINT: **An "unused" grep result is a hypothesis, not a finding** — check how an identifier
      is *constructed*, not just whether it appears as a literal.
- [ ] CONSTRAINT (new, 2026-09-03): **A test that renders a component without the hooks the app mounts
      beside it can be a false green** — the key-segment pin test passed while the feature re-pinned in
      the real app because `usePinRelease` lives in a sibling. Mount the hook (or both sections) in
      integration-shaped tests.
- [ ] CONSTRAINT (re-earned): **Plan-written test fixtures are not real fixtures** — two SubHover
      literals in the plan were invented and slipped into a file whose header forbids that. Every
      fixture must trace to a real `getInchi()` string already in the file.

**Do not proceed until all boxes are checked.**

<current_state>
No active GSD milestone. Branch **`feat/audience-toggle`** (off `dev` at bf78f0d) carries the
audience-register feature: 30 commits, HEAD `e31863d`, pushed to BOTH remotes, **PR #3 open against
`dev`** (https://github.com/Beilstein-Institut/explain-that-inchi/pull/3).
On the branch: `npm test -- --run` 1036/1036 · `npx tsc -b` clean · `npm run lint` clean · `npm run build` ok.
`dev` is unchanged apart from this handoff commit. `master` still at 2b7720d.
Nothing is in flight; no half-finished edit anywhere.
</current_state>

<completed_work>
Feature: **Expert / Simple explanation register + glossary + pinnable InChIKey.**
Spec (5 amendments): `docs/superpowers/specs/2026-09-03-audience-toggle-design.md`.
Plan (11 tasks + amendments 2b, 12, 13): `docs/superpowers/plans/2026-09-03-audience-toggle.md`.

- Store `audience: 'chemist' | 'plain'`, read from / written to `?mode=plain` (`src/lib/audienceUrl.ts`). No storage.
- `AudienceToggle.tsx` radiogroup (Expert / Simple) first in `.section-label-actions`, pill style of Reset/Help.
- Every copy table is a `Copy = { chemist, plain }` pair resolved with `pick()` (`src/lib/audience.ts`):
  `LAYER_INFO`/`DEFAULT_INFO`/`EMPTY_INFO`, `KEY_ZONE_COPY`, `subTokenInfo(sub, atoms, audience)`,
  tour `STEPS`, legend `ALL_LAYERS` (now exported). Chemist entries carry Blue Book section comments.
- `copyTables.test.ts` enforces the plain register (banned jargon, ≤ 20 words/sentence, registers differ).
- `glossary.ts` (~60 terms, `markTerms`) + `Prose.tsx`: underlined terms, tooltip portaled to body,
  fixed-position, viewport-clamped, flips by measured height, z-index 10000; hover/focus opens,
  click sticks; Escape propagates unless sticky; scroll/resize close.
- InChIKey segments pinnable (`keyPinned`, `setKeyPinned`, `clearKeyPinned`); release logic shared in
  `src/hooks/usePinRelease.ts`; clicking a pinned target (layer, sub-token, key) now releases it —
  the layer/sub-token re-pin was a pre-existing bug. `setHover`/`setSubHover` gated on either pin.
- Chemistry fixes from review: m-layer 0/1 was reversed; q-layer no longer names zwitterions;
  stereo sentence corrected for meso compounds; racemate citation → P-93.1.3.
- CLAUDE.md documents `?mode` and the toggle location.

Review trail: one spec+quality review per task, fable-advisor whole-branch review ("ship with
fixes", all fixed), scoped re-reviews of every fix wave. Full ledger with rulings and deferred minors:
`.superpowers/sdd/2026-09-03-audience-toggle/progress.md` (gitignored).
</completed_work>

<remaining_work>
None on the feature. Merge is the user's call (PR #3).
</remaining_work>

<decisions_made>
See HANDOFF.json "decisions". Headlines: URL not storage; ids chemist/plain with labels Expert/Simple;
Blue Book governs chemist terms; no hover-grace for glossary terms (pin first); m0 = as written;
pin targets exempt from capture release; Superpowers SDD used at the user's request instead of GSD.
</decisions_made>

<blockers>
None technical. Human gate: the browser checklist has not been walked end to end.
</blockers>

## Required Reading (in order)
1. This file.
2. `.planning/HANDOFF.json` — human_actions_pending, decisions.
3. `docs/superpowers/specs/2026-09-03-audience-toggle-design.md` (incl. amendments 1–5).
4. `.superpowers/sdd/2026-09-03-audience-toggle/task-11-report.md` — the browser checklist.
5. `.superpowers/sdd/2026-09-03-audience-toggle/progress.md` — rulings + deferred minors.

## Human Actions Pending
- **Blocking:** walk the browser checklist (task-11-report.md) on `feat/audience-toggle`.
- Approve `ALL_LAYERS` export in `Legend.tsx`.
- Review/merge PR #3; afterwards `rm -rf .superpowers/sdd/2026-09-03-audience-toggle`.
- Triage deferred minors (PR body + ledger).
- Carried over: wasm load-path browser check; fast-forward master, rebuild Docker, redeploy.

## Infrastructure State
- Remotes: `origin` = Beilstein-Institut org, `cm-beilstein` = personal mirror. Push both.
- Node 22 (`.nvmrc`); `npm test` bakes in `--no-experimental-webstorage`.
- No headless browser in the dev environment (no playwright/puppeteer installed — do not install without asking).
- Blue Book text extracts were in the session scratchpad only; regenerate with `pdftotext -layout` from https://iupac.qmul.ac.uk/BlueBook/PDF/P7.pdf, P8.pdf, P9.pdf if needed.
