---
context: default
phase: none
task: null
total_tasks: null
status: paused
last_updated: 2026-09-02T12:34:24.898Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Carried forward from the 2026-08-26 handoff. Each was discovered through actual failure in this
> repo, not predicted. Acknowledge each before proceeding.

- [ ] CONSTRAINT: **Never `git checkout <file>` while work is uncommitted** — it once wiped a whole
      pass's edits to `KetcherPanel.tsx`. Mitigation: mutation-test against a copy in the scratchpad,
      or commit before breaking things on purpose.
- [ ] CONSTRAINT: **Verify every commit in isolation when splitting a tree** — hunk staging once merged
      adjacent hunks from different changes into one broken commit. Mitigation: after each commit,
      `git stash -u` the remainder and run `npx tsc -b` + the suite.
- [ ] CONSTRAINT: **An "unused" grep result is a hypothesis, not a finding** — `.mol-item` is built by
      string concatenation in `KetcherPanel.tsx`. Mitigation: check how an identifier is *constructed*,
      not just whether it appears as a `className="…"` literal.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Untested guard | A test written alongside a fix can pass for the wrong reason. Earned again this session: every guard in 260902-gxx/hzh/i2t was mutation-tested. | blocking | Revert the fix, watch the specific test fail, restore. The SUMMARY of each quick task records which mutation was run. |
| Fabricated InChI fixture | 333 green tests once masked a broken feature (v1.4). | blocking | Every fixture is real `getInchi()` output. The b-layer tests use fumaric `/b2-1+` and maleic `/b2-1-` measured through the WASM. |
| Static evidence for a runtime claim | Unit tests cannot see the Ketcher canvas. This env has no WASM. | advisory | Say plainly when a conclusion is derived, and list the browser check in Human Actions. |

<current_state>
No active milestone since v1.6 (shipped 2026-06-30). This session was five `/gsd-quick` tasks driven
by user reports and by the 2026-06-15 `REVIEW.md` findings.

**13 commits on `dev` since the last handoff (cfb323f), HEAD `b6dbf9b`, pushed to `origin/dev`.**
Tree clean apart from three untracked files (see Infrastructure State).
`npx tsc -b` clean · `npm run lint` 0 problems · `npm test -- --run` 731/731 · `npm audit` 0 vulnerabilities.
`master` still at `2b7720d` — nothing since August is on master.

Nothing is in flight. There is no half-finished edit anywhere.
</current_state>

<completed_work>

```
6d212f9  fix: Set the mobile-H token in a real italic                    (quick 18)
27663f9  Model the b-layer token as a double bond, not a stereocentre    (260902-gxx)
09dc51a  Reset hover state when the InChI data changes                   (260902-gxx)
9806032  Clear the last two lint warnings                                (260902-hrp)
af47f98  Pin Node 22 and make the test script correct on newer Node      (260902-hrp)
73359c3  npm audit fix: browserslist and nanoid (dev tree)               (260902-hrp)
4c53ba7  Keep the legend inert while the canvas is empty                 (260902-hzh)
6292eee  Grey out the legend while the canvas is empty                   (260902-i2t)
```
plus one `docs(quick-…)` commit per task (plan, summary, STATE row).

**260902-gxx — b-layer sub-token model + hover reset.** `case 'b'` in `LayerText` no longer shares
`ParityText` with `/t`. New `BLayerText` renders each `a1-a2sign` token as ONE hover target with a new
`SubHover` kind `'bondStereo'` (`stereoBond: [a1, a2]` global canonicals + `sign`). `buildSubHoverSpecs`
lights both atoms and the bond in the parity colour. `subTokenInfo` gets a "Double-bond stereo" card
that calls the sign a canonical parity and explicitly NOT the E/Z descriptor. `parseBondStereoEntries`
accepts `?`; layer-wide `/b` highlight puts `?` bonds in the neutral stereo colour. `setInchiData` now
also clears `hoverIdx` and `subHover`. Closes REVIEW H-01, W-01, W-03. 712 → 726 tests.

**260902-hrp — hygiene.** Lint 2 warnings → 0. `.nvmrc`=22, `engines.node`=22.x (matches Dockerfile).
`npm test` bakes in `NODE_OPTIONS=--no-experimental-webstorage` (Node 25 global localStorage shadows
happy-dom and broke `leaveWipe` tests). `npm audit fix` lockfile-only → 0 vulnerabilities.

**260902-hzh — legend inert on empty canvas.** User report: hovering a legend row with no molecule
changed the explanation card. The shared `show` handler in `Legend.tsx` returns early when
`layers.length === 0`, before any store write. Leave/blur unchanged; rows stay focusable. +2 tests.

**260902-i2t — legend greyed on empty canvas.** `Legend.tsx` sets `data-empty="true"` and rows get
`tabIndex -1` while empty. `Explanation.module.css` `.legendCard[data-empty="true"]` mirrors
`.inchiDisplay[data-empty="true"]` (opacity 0.45, `--line-soft` border, `pointer-events: none`).
`src/testing/cssProbe.ts` `rule()` now escapes regex metacharacters and accepts a rule preceded by a
comment, otherwise attribute selectors could not be probed. +3 tests → 731.

**Quick 18 (2026-08-31).** Mobile-H `(H,…)` token set in a real Plex Mono italic; italic dropped on
the not-present note.
</completed_work>

<remaining_work>

No planned work. Open items are all human checks or decisions — see Human Actions below.
Candidate next milestone: none scoped; run `/gsd-new-milestone` if one is wanted. `ROADMAP.md` has a
post-v1.6 section with ideas.
</remaining_work>

<decisions_made>

- **/b token = one hover target with both bond ends**, kind `'bondStereo'`, not two `'stereo'` atoms.
  The `-` between atom numbers in `/b` is a separator; `ParityText` mis-signed the first atom of every
  pair (REVIEW H-01).
- **/b card says canonical parity, NOT E/Z** — same chemical-correctness rule as `/t` (parity from
  canonical numbering + geometry, not CIP).
- **Empty-canvas legend is both inert (handler guard) and visibly greyed (CSS)**, mirroring the InChI
  box. The explanation card itself is untouched, so precedence once a molecule exists is identical
  (UAT-13).
- **Pin Node 22, bake `--no-experimental-webstorage` into `npm test`** rather than documenting an env
  var. Dockerfile already used `node:22-bookworm-slim`.
- **Untracked `REVIEW.md` and the CICAG article draft were deliberately not committed** by this handoff.
</decisions_made>

<blockers>
None technical.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — Key Decisions (carry-forward) and the Quick Tasks table; the invariants
   there (verbatim-passthrough, no-remount, never map +/− to R/S) are hard.
2. `.planning/quick/260902-gxx-*/…SUMMARY.md` — the new `bondStereo` kind and what it must never say.
3. `.planning/quick/260902-i2t-*/…SUMMARY.md` — the `cssProbe` change if you touch CSS tests.
4. `.planning/REVIEW.md` (untracked) — 2026-06-15 full-tree review. H-01, W-01, W-03 are now fixed;
   check the medium items before assuming any is still open.
5. `CLAUDE.md` — GSD enforcement, nginx COOP/COEP gotcha, rejected choices.

## Human Actions Pending
1. **(blocking, carried over)** Browser-verify the binary Indigo wasm load path (`aa60971`) in a fresh
   profile behind nginx COOP/COEP. Still the one unverified runtime change with failure potential.
2. Browser-verify `/b` hover on fumaric acid, maleic acid, prostaglandin E₂ presets: one hover target
   per bond token, both atoms + bond lit in the parity colour, card wording canonical-parity-not-E/Z.
3. Browser-verify the empty-canvas legend: greyed at 0.45 with ring swatches still discernible, rows
   skipped by Tab, hover inert until a molecule exists.
4. Decide on the three untracked files (below).
5. When checks pass: bring `master` up from `dev`, rebuild the Docker image, redeploy by hand.

## Infrastructure State
- Branch `dev` = `origin/dev` = `b6dbf9b`. `master` = `2b7720d`. Only remote is `origin` (GitHub).
- Untracked, NOT committed: `.planning/REVIEW.md`, `docs/cicag-distillate-article.md`,
  `docs/cicag-distillate-article.docx`.
- Local Node is 25; `.nvmrc` says 22. `npm test` works on either now.
- No dev server or watcher running. No WASM in this environment — canvas behaviour cannot be observed here.
- Deploy target: Docker + nginx → `https://cheminfo.beilstein.org/explain-that-inchi/`, by hand.

<context>
Maintenance mode. The pattern this session: user reports a UI oddity → `/gsd-quick` → planner →
executor → mutation-tested guard → docs commit with STATE row. `REVIEW.md` still lists medium items
(type-safety, test gaps) that nobody has triaged since June; that is the obvious next sweep if there is
no feature work. Every quick task's SUMMARY ends with the human check still open.
</context>

<next_action>
Start with: `nvm use && npm run dev`, then work through Human Actions 1–3 in the browser. Then decide
whether `.planning/REVIEW.md` and the CICAG article draft get committed.
</next_action>
