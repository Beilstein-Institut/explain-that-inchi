---
target: the app (src/App.tsx)
total_score: 24
max_score: 40
na_heuristics: 
p0_count: 2
p1_count: 3
timestamp: 2026-08-06T11-30-59Z
slug: src-app-tsx
---
Method: dual-agent (A: a88801f5f54f89e52 · B: a5d631786ee474f94). No browser automation available on this machine; no overlay was produced.

## Design Health Score — 24/40 (Acceptable)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 2 | Rejected InChI blanks the store; strip says "Draw a molecule above" while the molecule is on canvas |
| 2 | Match System / Real World | 3 | Every layer has two names (legend vs card) |
| 3 | User Control and Freedom | 2 | Any document click releases the pin, including the canvas click pinning exists to enable |
| 4 | Consistency and Standards | 3 | Help and Reset are byte-identical pills; one onboards, one destroys |
| 5 | Error Prevention | 2 | Reset has no confirm, no undo, and a twin |
| 6 | Recognition Rather Than Recall | 3 | Card-to-chunk link is a 3px stripe; sub-token cards never name their parent layer |
| 7 | Flexibility and Efficiency | 2 | No paste-InChI input; no URL state; no preset search (keyboard nav now real) |
| 8 | Aesthetic and Minimalist Design | 3 | Same 30-word tagline on all four InChIKey cards; legend holds 50% of teaching row |
| 9 | Error Recovery | 1 | Silent failures: WASM error, InChI rejection, clipboard. No error surface anywhere |
| 10 | Help and Documentation | 3 | Tour steps 3-5 spotlight the same element; no scrollIntoView |

## Design Specificity Verdict — split

Authored: one hue per layer solved to a matched lightness band; Jmol CPK element colours with H forced to --ink; H-count as a lightness ramp; parity sign as a two-colour axis. Every hue answers to a chemical fact.

Generic: .explain 50/50 grid, bordered card, generic coachmark tour, stock header/footer/feedback modal.

The mismatch is the finding: the most product-specific artefact (the coloured string) carries no heading, while the most generic (a bordered card) gets half the teaching row. .sectionLabel/.hint exist in InchiSection.module.css:13-30 and no component renders them.

Detector: 0 findings with project config; 3 without, all previously waived. layout-transition waiver verified correct (styles.css:350 is `transition: stroke, stroke-width` on SVG bonds; detector snippet truncates). Caveat: waivers use value:"*", so future genuine hits in those files are also swallowed.

## Priority Issues

- [P0] No way to paste an InChI in. PRODUCT.md's primary user arrives holding a string the tool cannot receive. Fix: paste field + ?inchi= URL param; parseInchi() already exists. -> /impeccable shape
- [P0] Empty state lies on parse failure. App.tsx:185-195 blanks the store; InchiSection.tsx:89 shows "Draw a molecule above" with a molecule on canvas. Fix: add inchiError state, distinct copy. -> /impeccable harden
- [P1] Any click releases the pin including canvas clicks. InchiSection.tsx:42-54. Contradicts tour step 5. Fix: scope the release to outside canvas + strips. -> /impeccable harden
- [P1] Explanation card not adjacent to the string. DOM order puts InchiKeySection between them; highlight is far above cursor, card far below. Fix: move Explanation above InchiKeySection; consider card beside canvas, legend demoted to chip strip. -> /impeccable layout
- [P1] Hierarchy inversion: Send feedback is the only saturated button; Help is an invisible grey twin of destructive Reset. Fix: demote feedback, promote Help, differentiate + confirm Reset, kill the empty state by auto-loading caffeine. -> /impeccable layout

## Persona Red Flags

- Jordan (first-timer): empty canvas, no definition of InChI, strips at opacity 0.45 read as disabled, idle card says "hover any layer" when none exist, only bright button says "Send feedback".
- Alex (power user): no paste field (dead stop ~4s), 20 presets with no search, no URL state to share, pin destroyed by canvas click.
- Sam (student, from PRODUCT.md): real peak on first sweep; then c-layer card teaches the borane exception before the rule, h-layer card packs four concepts in one paragraph, InChIKey repeats the same tagline four times.

## Minor Observations

- The reduced-motion block added today is a no-op: .legend-tip exists only in styles.css:665-713, no component renders it (removed UAT-13). Either delete or retarget to the HelpTour spotlight.
- ~250 lines of dead global CSS in styles.css shadow the live CSS modules (.inchi-display, .explain, .card, .legend-row). Two sources of truth.
- aria-label on layer chunks announces the raw mono string first; human name should lead.
- 11 legend rows are now tab stops with no action.
- Header.tsx:8 hard-codes "InChI v1.06" against the project's verbatim principle.
- HelpTour claims aria-modal with no focus trap or restoration.
- Empty strip is pointer-events:none, so the copy button is unreachable.
- cursor:crosshair on sub-tokens that are now role="button".

## Questions to Consider

1. If the primary user arrives holding an InChI, why is the canvas the front door?
2. Why does the explanation live in a card rather than inline under the hovered chunk, like a critical edition's gloss?
3. Does anyone leave having learned anything? Nothing ever asks the user to say something back.
4. What is the legend for, once the string is coloured and hoverable?
